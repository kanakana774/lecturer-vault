> 水谷さん向け / **手順だけを書いた1枚。** 根拠・実測・判断の経緯は載せていない。
> **2026-09-14 手順に絞って作り直した。**何を足すかはこれから決める。
> 設計原則は `06_研修の芯.md`、根拠つきの旧版は git 履歴（`6811daf` 以前）にある。

# AI研修の流れ

## 目的

> ### AIに書かせたコードの品質を、自分で担保できるようにする。

SES では客先に**自分の名前で**成果物を出す。AIが書いたものでも変わらない。

対象は**3か月目の新人**。期間は**1週間**。
題材は**2か月目と同じ Todo API**（Java 21 / Spring Boot 3.4.1 / MyBatis）。
Todo と Tag の CRUD ＋ Todo の登録／更新時に Tag を紐づける機能。

---

## 研修生に達成してもらいたいこと — 3点

|       |            | 到達した状態                          | どこで     |
| ----- | ---------- | ------------------------------- | ------- |
| **1** | **コンテキスト** | **何を渡し、何を渡さなかったかを、理由とともに説明できる** | **A**   |
| **2** | **逸脱の把握**  | **AIが設計から逸れた箇所を、説明できる**         | **B・C** |
| **3** | **線引き**    | **担保できた範囲と、できない範囲を言える**         | **C**   |

**3点とも「独断しないこと」が合格条件。判断の広さは求めない。**

---

## 全体

```
【前提】
  ガバナンス  ──  理解度テストに合格  →  本編

【本編】
  入口（自分）      →      中（AI）      →      出口（自分）
   A 入力を整える          B 任せる            C 確かめる
      半日                 半日                 半日
```

**実装するのはAI。研修生がやるのは、入力を整え、任せ、確かめること。**
**各段の提出物が、次の段の入力になる。**

---

## 作らせるもの — 6点

| 作らせるもの | どこで | AIの扱い | 何のために |
|---|---|---|---|
| **直した設計書（差分）** | **A-2 →（B・C で増える）** | **自由** | **矛盾と未決を、実装より前に消す** |
| **`確認事項.md`** | **A-2 →（B・C で増える）** | **自由**（B の逸脱行はAIが書く） | **確認した事実と、決まらなかったことを残す** |
| **`テストケース一覧.md`** | **A-3** | **★AIに作らせ、過不足を人が見る** | **合格の基準を、実装より前に固定する** |
| **`実装計画.md`** | **B-0** | **★AIに書かせ、承認してから実装** | **触る範囲を、先に宣言させる** |
| **PR（レビューと仕分け）** | **C-2・C-3** | **★C-2 はAIを使わない** | **担保できた範囲と、できない範囲を示す** |
| **`振り返り.md`** | **通し** | **★AIに渡さない** | **何を渡し、何を渡さなかったかを言わせる** |

**上の4つは、実装より前に作らせる。早くやるためではなく、汚染される前に固定するため。**

### ★ が指示。無印は自由

**指示は「何を作るか」と「AIに任せてよいか」まで。「どう作らせるか」は縛らない。**
どう渡し、どう確かめたかは研修生が決める。**そこが評価の材料になる**（→ 評価）。

**指示には2種類ある。研修生にはどちらかを言う。**

|  | どういうものか | どれ |
|---|---|---|
| **実務の作法** | **やり方が実務でほぼ1つに決まっている。必ず踏ませる** | **テストケース一覧をAIに作らせる／実装計画を承認してから実装する／B の4手** |
| **研修の都合** | **外すと到達点3点が測れなくなる。「実務の作法ではない」と断って渡す** | **C-2 でAIを使わない／`振り返り.md` をAIに渡さない／`実装計画.md` をファイルで残す** |

**やり方が決まっていないものは指示にしない。**講師の案は**模範アプローチ**として、本人がやりきったあとに見せる。

---

## ⓪ ガバナンス（前提）

1. 客先のルールを確認する
2. 渡してはいけないものを渡さない
3. 何を渡したか記録する
4. AIに書かせた部分を PR で明示する

**あわせて B の4手も、ここで説明しておく。**先に失敗するテストを書かせ、通してコミットする——
**`CLAUDE.md` に書いてあるので、説明さえすれば勝手に回る。**

**理解度テストに合格して、次に進む。**

---

## A 入力を整える（半日）

**課題で配られるものと、AIに最終的に渡すものは別物。その差分を作るのが A。**

|         | やること                                     | 出るもの                        |
| ------- | ---------------------------------------- | --------------------------- |
| **A-1** | 配られたものを確認する（設計書一式・`CLAUDE.md`・雛形）        | —                           |
| **A-2** | **AIに未決事項を列挙させ、仕様管理者（講師）に聞き、設計書を直す**     | 直った設計書 ／ `確認事項.md`          |
| **A-3** | **AIにテストケース一覧を作らせ、抜け漏れを見る**              | `テストケース一覧.md`               |

- **ケースは1件ごとに「入力・事前状態・期待する結果」＋ 設計書のどこから来たかの根拠を書く**
- **期待する結果を断定できないものは、ケースにしない。**`確認事項.md` に戻して A-2 へ
- **やり方は指定しない。**どう辿ったかを、あとで議論する

### B に進んでよい状態

> ## **テストケース一覧に合意できている。**

**合意した時点が着手基準。**研修生が自分でその場で確かめられる。
**半日で届かなければ、残りを「決まらないと知って進む」に落として B に行く。それも正しい終わり方。**

---

## B 任せる（半日）

**計画を1枚書いてから、1コミットずつ4手を回す。**

### B-0 ブランチを切って、実装計画を書く

> **配布時点から `feature` を切り、そこから `task` を切って作業する。`main` には直接コミットしない。**
> **実装の前に、`実装計画.md` に3節（変更するファイル／作業の順序／リスク）を書く。承認するまで実装しない。**
> **plan モードを使ってよい。ただし計画はリポジトリに残らないので、承認のあとに同じ内容を `実装計画.md` へ書かせる。**

### B-1〜 コミットごとの4手

|     | やること                                               | 誰が  |
| --- | -------------------------------------------------- | --- |
| ①   | **そのコミットで満たすケースを、`テストケース一覧.md` からテストに書く**（実装ではなく仕様から書く） | AI  |
| ②   | **実装前に `mvn test` を走らせ、①のテストが失敗する出力を見せる**          | AI  |
| ③   | **実装する。①のテストは変えない**（変えたいときは変えずに理由を報告）              | AI  |
| ④   | **`mvn test` と `mvn checkstyle:check` を通してコミットする** | AI  |

- **コミットはエンドポイント単位で刻む**（試走では9本）。**Tag を先に、Todo を後に**
- **4手は `CLAUDE.md` に書いて渡す。**研修生から見える手数を増やさない
- **auto を使ってよい。ただし取り消しがきかない操作（コミット・ファイル削除・DB操作）では承認を取る**
- **リモートはまだ足さない。**提出は C-2。足すのは講師

### 設計から逸れたら、設計書に書き戻す

> **設計書と違う実装にしたら、設計書を直し、`確認事項.md` に1行足してから報告してください（「いつ」列は `実装中`）。**

**「いつ」が `実装中` の行が、そのまま「AIが設計から逸れた箇所」**（→ 到達点2）。
**逸れたものは全部上げる。「自分で決めてよい逸脱」は無い。**

**確認を待たずに書き戻す。**設計書も git 管理なので、暫定で直しておけば差分に残る。
**確認は `確認事項.md` の「回答」列で追い、答えが違っていたら直し直す。**
**回答が空のまま残った行は、C で答え合わせされる。**

**そこで実装を止めて聞くか、作りきってからまとめて聞くかは指定しない。**現場のスタイルによる。


---

## C 確かめる（半日）

> ### AIが書いたコードを、問題ないと自分で説明できる状態にする。

| 段                          | やること                                                         | 誰が            |
| -------------------------- | ------------------------------------------------------------ | ------------- |
| **C-1 機械チェックの妥当性を確かめる**    | **ケース一覧 ↔ テストの対応を見る。「このテストは効いている」と言える根拠を出す**                 | **AIにやらせてよい** |
| **C-2 コードレビューをする**         | **変更範囲**（`実装計画.md` が参照点）・**DoD 9項目**・その他気になるところ。**結果を PR に書く** | **AIを使わない**   |
| **C-3 AIにもレビューさせ、仕分ける**    | **正しい／的外れ／判断できない に分け、処置を決めて PR に書く**                        | AI ＋ 自分       |

**C-2 だけは AI を使わない。**研修全体で、自分の目でコードを読む工程はここだけ。

### C-3 の仕分けと処置

| 判定           | 処置                                                       |
| ------------ | -------------------------------------------------------- |
| **正しい**      | **直す → コミット → push → 再レビュー。**直すときも **B の4手**             |
| **的外れ**      | **理由を書いて閉じる。**直さない                                       |
| **判断できない**   | **消さない。**仕様なら `確認事項.md`（「いつ」列は `確認時`）→ 仕様管理者へ。技術ならPRに書いてレビュアーへ |

**再レビューは1周で切る。**2周目の新しい指摘は仕分けるだけ。
**終わりの条件: 3分類すべてに処置が決まったら終わり。**「正しい」を全部直した、ではない。

### 終わりの形

**自分で確かめた範囲と、仕様管理者に問い合わせる範囲を、線を引いて言えること。**

> **自分の観測で決着するものは、全部自分側。他人の同意が要るものは、全部相手側。**
> **難しいかどうか、時間がかかるかどうかでは切らない。**

---

## 評価

|             | 測り方                                   |
| ----------- | ------------------------------------- |
| **ガバナンス**   | **理解度テスト。合格が必要**                      |
| **A**       | **アプローチを議論する。**成果物の正しさは測らない           |
| **B**       | 逸れた箇所を全部上げたか（`確認事項.md` の「いつ＝実装中」の行）   |
| **C**       | **PR と `振り返り.md` を読んで、議論を返す**         |

**添削はしない／評価はする／議論はする。** 3つ目が実質のフィードバック。
**模範アプローチは、本人がやりきったあとに一例として見せる。**

---

## 配布物

**リポジトリは2つだけ。最後まで増えない。★ = AI研修のために足すもの**（2か月目の環境には無い）。

```
todo-app-api/                        ← 実装が入る
├── ★ CLAUDE.md                      AIへの指示ファイル。DoD 9項目・B の4手を含む
├── ★ .claude/settings.json          AIに許す操作の範囲。触らない
├──   pom.xml ／ todo_checks.xml     2か月目と同じ ＋ ★checkstyle
└──   src/
     ├── 2か月目の雛形そのまま      空フォルダ・共通例外・SecurityConfig・testTools
     └── ★参照実装1本               Todo 1件取得の完成品 ＋ 統合テスト1本

todo-app-api-docs/                   ← ★研修生が書き戻す（読むだけではない）
├──   要件定義書・アーキ図・DB定義書・OpenAPI・エラーコード表・プログラム設計
├── ★ 確認事項.md                    ★空の雛形。A・B・C で行が増える（提出物）
├── ★ テストケース一覧.md            ★空の雛形。A で埋める（提出物）
└──   実装計画.md                    ★配らない。B の頭でAIが書く（提出物）

（リポジトリの外・別配布）振り返り.md   ★問い5つだけの雛形。AIに渡さない（提出物）

（リモートは C-2 で講師が足す）        ★AIには最後まで触らせない
```

---

## 今後の予定

| いつ           | 何を                            |
| ------------ | ----------------------------- |
| **9/14週**    | **1周分の試走は完了**                 |
| **9/28週**    | **バッファ**                      |
| **10/12週**   | **ここまでに完成**（配布物・進行ガイド・理解度テスト） |
| **10月末の最終週** | **初回実施**                      |
