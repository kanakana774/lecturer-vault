# 完了の定義（DoD）

**すべての変更に共通の最低ライン。** 機能ごとの受入条件とは別に、**必ず満たす**。

> **これは講師（チーム）が配るもの。** 研修生が決めるのは機能ごとの受入条件で、この一覧は与えられる側。
> 実務でも同じで、DoD はチームが持ち、参画者は渡される。

## 機械が判定する

- `mvn test` が通る
- `mvn checkstyle:check` が通る

## 人が見る

- **for 文の中で SQL を呼び出していない**（件数分のクエリを発行しない）
- **例外メッセージ・スタックトレース・SQL・設定値を外に出さない**
- **層構成を維持する**（Controller / Service / Mapper の責務を越えない）
- **見せてはいけない情報をレスポンスに含めない**（内部ID・設定値・DB都合のカラム）
- **`SELECT *` を使わない**
- **頼んでいないファイル・テーブルを変更しない**
- **依存（`pom.xml`）を勝手に追加・変更しない**

---

> **出典**: 「人が見る」の**7項目**は**2か月目のレビュー観点そのまま**。新しく覚えるものはない。
> **★この「人が見る」欄が、段④の目視チェックリストそのもの。** 一巡したら終わり、が停止条件になる
> （`11_品質特性と検証の分担.md` §8）。**項目を減らすと停止条件が痩せる。**
>
> **⚠ 2026-08-26 に一度「安全と作業規律だけ」に絞り、同日中に戻した。**
> 絞った動機は「DoD に書くと AI が先回りして守り、段④の題材が消える」だったが、
> **DoD から外して再実測しても N+1 は1つも出なかった**（`10_シミュレーション/03_委任_出てくるもの.md` ★9）。
> **動機が反証された一方、`11_` §8 の停止条件としての役割が残るため、元に戻した。**
