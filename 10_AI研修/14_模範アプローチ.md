> **講師用。研修生には配らない。** 示すのは**本人がやりきったあと**（`06_研修の芯.md` 設計原則2）
> 流れは `15_通しの流れ.md`／実際に1周回した記録は `16_見本_Aの1周.md`
> 出典: [The AI-native SDLC Playbook](https://claude.com/blog/the-ai-native-sdlc-playbook)（Anthropic, 2026）

# 模範アプローチ

**A / B / C それぞれの一例。** **唯一の正解ではない。**

| | 状態 |
|---|---|
| **A 入力を整える** | **書けている**（下記・6手） |
| **B 任せる** | **`CLAUDE.md` の4手がそのまま模範。**別に書くことが無い |
| **C 確かめる** | **書けている**（下記・2026-09-10〜11 の実測） |

---

# A 入力を整える

## これは何か

| | |
|---|---|
| **示すもの** | **唯一の正解ではなく、アプローチの一例** |
| **示すタイミング** | **本人が自分のやり方を出しきったあと。**議論のあと |
| **何のために** | **自分のやり方と比べる材料。**比較があって初めて議論になる |
| **⚠ 置き場** | **リポジトリに置かない**（`03_研修環境とガードレール.md` §8。AIが勝手に読みに行く） |

**「答えを配らない」は破っていない。** 配るのは答えではなく議論の材料。

---

## Playbook のどこを使ったか

**Playbook は6ステージ（01 Plan / 02 Design / 03 Build / 04 Test / 05 Deploy / 06 Maintain）に分かれ、各ステージが「プレイ」を持つ。**

> **"Each play covers: What changes; Getting started; Concrete steps for implementation; Governance considerations; and How you measure whether it worked."**

**A に効くのは4つのプレイだけ。ステージはまたいでいる**（A は Playbook の1ステージに対応しない）。

| Playbook のプレイ（見出しのまま）                                   | ステージ      | 研修の手                |
| ------------------------------------------------------- | --------- | ------------------- |
| **Requirements and design**                             | 02 Design | **手1〜3**（設計書を精緻化する） |
| **The CLAUDE.md**                                       | 03 Build  | **手4**              |
| **Give Claude a feedback loop**                         | 04 Test   | **手5**（**考え方だけ。**ケース一覧という形は Playbook に無い） |
| **Claude Code plan mode as the default starting point** | 03 Build  | **手6**（着手判定）        |

**★手3（フレームワークの既定を挙げさせる）に対応するプレイは Playbook に無い。** こちらの実測から起こしたもの。

> [!warning]- 出典の精度について（2026-09-01）
> **初稿では「Stage 3 に `Before Delegating to Claude` という4項目がある」と書いていたが、これは誤り。**
> **そのような見出しはページに存在しない。**要約を経由して読んだときに、要約側が作った区分だった。
> **見出しを取り直して差し替えた。**以降、この文書に `"..."` で囲んだ英文は**ページからの逐語引用**。

### Playbook の成果物と、研修の配布物の対応

| Playbook | 研修 | 誰が持つか |
|---|---|---|
| `intent.md`（何がしたいか） | **無し。**課題として与えられる | — |
| **`spec.md`（要件＋設計）** | **配られた設計書一式** | 仕様管理者（講師）。**研修生が書き戻す** |
| **`plan.md`（実装計画）** | **手6 で出させる計画** | 研修生が承認する |
| **`CLAUDE.md`** | **配る。研修生が足す** | 研修生 |
| `REVIEW.md`（レビュー方針） | 2か月目のレビュー観点で代用 | — |
| Skills / Hooks | **使わない**（後述） | — |
| **（対応なし）** | **`テストケース一覧.md`** | **★研修固有。**下記 |

### ★ `テストケース一覧.md` は Playbook に対応物が無い

**"acceptance criteria" も "definition of done" も、ページに一度も出てこない。**
**Playbook は完了の基準を2箇所に分けて置き、独立した一覧を持たない。**

| | どこ | 例（逐語） |
|---|---|---|
| **変更ごと** | **`plan.md` の Proof 節**（インライン。別文書を参照しない） | **"test_status.py covers the four claim states; screenshot matches the approved mock."** |
| **常に守る** | **`CLAUDE.md` の検証ブロック** | **"Test: make test (all green; never skip or delete a failing test)"** |

> **"Commit the approved plan as `plan.md`. The plan joins the audit trail, and the PR review play checks the eventual diff against it."**
> **完了の基準は `plan.md` 自身が持つ。**

**なぜ研修では1枚にまとめるか**

| | |
|---|---|
| **コミットが9本前後に分かれる** | **変更ごとの Proof だけだとコミットごとに散る。**C で全体を突き合わせられない |
| **実装を見る前に固定したい** | Proof は計画と同時に書くので、**実装の直前**になる。ケース一覧は**A のうちに合意する** |
| **C-2 の突き合わせ表になる** | **「一覧にあるのにテストが無い行」を数える**ための参照点 |

**⚠ DoD 9項目はここに混ぜない。** `CLAUDE.md` に置いたまま、C-2 で別に一巡する。**ケースではないので。**

---

## 6手

### ① 材料を集める

#### 手1 棚卸し — 何が配られたか、AIに一覧させる

> **「このリポジトリにある資料を全部挙げて、それぞれ何を決めている文書か1行で説明して」**

**出てくるもの:** 6つの設計書（要件定義書・アーキ図・DB定義書・OpenAPI・エラーコード表・プログラム設計）＋ `CLAUDE.md` ＋ 雛形2つ。

**なぜ効くか:** **読まずに済ませる資料を決められる。** `12` で「通読するのは要件定義書とアーキ図だけ」としているのは、**残りは必要になったときに開くほうが速い**から。何があるかを知らずには、その判断ができない。

**⚠ ここでアーキ図の「今回アーキテクチャを省略する」という注記を拾えるかが、後で効く。**
拾えていないと、C-3 で「Entity をそのまま返している」という**的外れな指摘**を正しいと誤判定する。

---

#### 手2 資料間の矛盾を洗い出させる

> **「6つの資料を突き合わせて、食い違っている箇所と、どこにも書いていない箇所を、根拠つきで全部挙げて。推測で埋めないで」**

**出てくるもの（実測ベース）**

| 出るもの | 根拠 |
|---|---|
| Todo名の物理名が違う | DB定義書は `NAME`、DDL は `title` |
| Tag削除時の `todo_tag` の扱い | DB定義書に記載なし |
| 同名のTagを登録できるか | UNIQUE 制約が無い |
| 進捗状況という項目 | 要件定義 3.1 にあるが DB設計にない |
| Tag削除の順序が FK の不在に依存 | DB定義書は複合PK、DDLは制約なし |
| 部分一致の `%` `_` の扱い | どこにも無い |

**★ここは AI が得意。人が6資料を突き合わせるより速く、漏れない。**
**「AIを使った効率的な仕様把握」の実体はここ。**

**⚠「推測で埋めないで」が要る。** 言わないと、AIは**もっともらしい答えを書いて矛盾を消してしまう**。

---

#### 手3 ⏸ この手は落とした（2026-09-04）

> **⚠ この問いは打たない。**フレームワーク名を並べられる時点で、**答えを半分知っている。研修生には打てない。**
> **手6 の「独断で決めることになる事項を列挙して」で同じものが出る**（実測で確認）。
> **実際の記録は `16_見本_Aの1周.md`。**以下は経緯として残す。

#### （旧）手3 フレームワークの既定が決めてしまう箇所を挙げさせる

> **「設計書に書いていないせいで、MyBatis / Jackson / Bean Validation / 既存の共通例外の既定が"決めてしまう"箇所を挙げて」**

**★これが一番差がつく手。** 手2 では出ない。**資料を突き合わせても出てこないから。**

**実測で出た5件は、全部この形だった。**

| 何が起きたか | 誰が決めたか |
|---|---|
| 重複行が畳み込まれた | **MyBatis の既定** |
| レスポンスに全フィールドが出た | **Jackson の既定** |
| 「30文字」の数え方 | **Java の既定**（サロゲートペア） |
| 型不一致で 500 が返った | **既存の共通例外** |
| 空白1文字が通った | **バリデーションの既定** |

> **`06_研修の芯.md`「決めなかった仕様は、既定の動作で決まってしまう」**
> **L1 の考慮漏れは「決まらないまま」では終わらない。必ず何かが決める。そして誰も担保していない。**

**この手を打たないと、5件は B で実装されるまで表に出ない。** 打てば、**A の時点で「未回答」として印をつけられる。**

**★A の良し悪しは、ほぼここで決まる。**

---

### ② 制約を書く

#### 手4 `CLAUDE.md` を仕上げる

**プレイ「The CLAUDE.md」の "How to execute it" は5手。うち3手が研修に効く。**

> - **"Cut the generated file down to what a new joiner would need on day one."**
> - **"When Claude makes a mistake twice, the correction goes into `CLAUDE.md`."**
> - **"Keep it under a page, because Claude reads all of it at the start of a session and anything stale is taking up context for no benefit."**

**★1つめが「新入社員が初日に必要なものまで削れ」。** 研修そのものの読み手と一致する。
**★3つめの「1ページに収めろ」が、skill 化を保留した判断の裏付けになる**（→ 後述の「あえて外したもの」）。

> [!note]- 2つめは研修では回らない
> **"When Claude makes a mistake twice"** は**同じ失敗を2回見て初めて足す**という手なので、**1週間・1周では回らない。**
> これは `06_研修の芯.md` の到達ライン **「1年 — 渡すコンテキストを育てられる」** の内容。**最後に一言で触れるだけにする。**

**配った時点であるもの:** DoD 9項目、3層構成、B の4手、`mvn` コマンド。

**研修生が足すもの（この課題で効くのは3行）**

| 足す行 | なぜ要るか |
|---|---|
| **「自分のローカルPCで動かす前提。デプロイ・スケール・監視は考慮しない」** | **書かないと考慮が無限に終わらない。**「デプロイ環境は？」と聞いてくる |
| **「足りない情報があれば、推測せず全部聞いてください」** | **`06`「AIは聞かなければ言わない」。**言わないと黙って決める。**手6 の前提** |
| **「参照するのはこのリポジトリと `todo-app-api-docs/` だけ。他のブランチは見ないでください」** | **`03` §8。**AIは渡していない情報源を自分から探しに行く |

**⚠ 3行目は保険。** 本体の対策は「**リモートは C-2 で講師が足す。AIには触らせない**」（`15` 参照）。

---

### ③ 完了を数える

#### 手5 テストケース一覧を作らせ、抜け漏れを見る

**プレイ「Give Claude a feedback loop」の "How to execute it" は7手。研修に効くのは3つ。**

> - **"State a target and make it quantifiable so Claude can check the work without asking you."**
> - **"Make verification part of 'done.'"**
> - **"Finally, the loop itself needs protecting, because an agent fixing code must not be able to weaken the check."**

**★1つめが「検証手段」列そのもの。** 数えられる形にしないと、AIが自分で確かめられない。
**★3つめが B-2 ③「①のテストは変えない」の裏付け。** 直す側が検査を弱められてはいけない。
**Playbook はここを hook で機械的に塞ぐが、研修では `CLAUDE.md` の1行にしている**（新規技術を増やさないため）。

**この手でAIに言うこと**

> **「設計書と参照実装から、テストケース一覧を作って。1件ごとに入力・事前状態・期待する結果を書いて。**
> **設計書のどこから来たかの根拠も添えて」**

**★「根拠」列まで書かせるのが肝。** 根拠が書けないケースは、**仕様がまだ決まっていない。**

```markdown
| # | 対象 | 入力・事前状態 | 期待する結果 | 根拠 |
|---|------|----------------|--------------|------|
| 2 | Todo一覧取得 | limit=1、Todoが2件以上存在 | 200。先頭1件のみ返却 | todo.yaml `limit`「取得上限件数」 |
| 6 | Todo一覧取得 | limit=0 | 400、badRequest.invalid-field | Todo機能.md「limitが1未満の場合400」 |
```

**★期待する結果が断定できないものは、ケースにしない。** → **`確認事項.md` に戻して手2へ。**
設計書に答えが無いか、設計書どうしが食い違っている、ということなので。

**★抜け漏れは、別のAIにレビューさせる。** ただし**仕分けまでで1セット**にする。

> **実測（2026-09-08）: 7件の指摘のうち対応したのは3件。残り4件を捨てた理由がどこにも残らなかった。**
> **見落としたのか、判断して捨てたのかが、あとから区別できない。**

**⚠ 別AIレビューは、痕跡のあるものしか拾えない。**
試走では、**設計書に矛盾の材料があった1件は拾い、どこにも書いていない2件（`%` エスケープ・パスパラメータ非数値）は0件**だった。
**→ 手3（既定が決めてしまう箇所を挙げさせる）の代わりにはならない。**

**⚠ 数え間違いに注意。** 実測では、AIが計画に書いた件数が**「59件超」「全62件」で、実物は69件**だった。
**C-2 で数を根拠にするなら、ここで合わせておく。**

---

### ④ 計画で確かめる

#### 手6 インタビューさせて実装計画を出させる（＝着手判定）

**プレイ「Claude Code plan mode as the default starting point」がそのまま使える。**

**何を出させるか（"How to execute it" より）**

> **"The engineer gives Claude the `intent.md` and the `spec.md` and asks for an implementation plan that names the files that change, the order of the work, and the tests that prove it."**
> （**変わるファイル／作業の順序／それを証明するテスト**を挙げさせる）

**どこまで往復するか — ★これが着手基準の言い換え**

> **"Iterate until an engineer who has never seen the conversation could implement the change from the plan alone."**
> （**その会話を一度も見ていないエンジニアが、計画だけで実装できる**ところまで往復する）

**「渡したものだけで実装が一意に決まる」＝「会話を見ていない人が、計画だけで実装できる」。**
**★研修生への説明は、この言い方のほうが伝わる。**

> [!note]- Playbook の前提と、研修の前提の違い
> Playbook は plan mode の **Prerequisites** をこう書いている。
> **"The intent artifact (`intent.md` or `spec.md`) if one exists, and the `CLAUDE.md` file helps."**
> **"if one exists"** ——**無くても始められる**前提。**研修では設計書が必ず配られる**ので、ここは研修のほうが条件が良い。
> **だから研修の焦点は「spec を作る」ではなく「配られた spec を、渡せる状態まで精緻化する」**になる。

**やること — 4手**

**(0) ★ まず、決めることになる事項を直接列挙させる**

> **「実装するとして、あなたが独断で決めることになる事項を列挙して。実装はしないで」**

**AIは生成が得意で、自分から止まるのが苦手。** その性質を逆に使う。
**「不明なら聞け」という指示は効果が薄い**（何が不明かをAI自身が判定できないため）。**列挙させるほうが直接的。**
**列挙された項目に自分が即答できなければ、準備不足。**

**⚠ 列挙ゼロは準備完了の証拠にならない。** 網羅性は保証されない。**だから (1)〜(3) も要る。**

**(1) 新しいセッションを開き、渡すものだけを見せて実装計画を出させる**

> **「実装計画を出して。下の4節で書いて。足りない情報があれば推測せず全部聞いて」**

```markdown
## 変更するファイル
## 作業の順序
## リスク
```

**★「証明」節は落とした（2026-09-04）。** 何をテストするかは `テストケース一覧.md` にあるので、**計画に書き写すと二重管理になる。**
Playbook の `Proof` は**テストを名指しするだけで別文書を参照しない**ので、外しても Playbook から離れない。

**⚠ 「変更するファイル」は C-1 の参照点になる。** ここが無いと**「頼んでいないファイルが変わっていないか」を言えない。**

> [!warning]- 「リスク」節がエスカレーションを吸収する（2026-09-08・実測）
> **試走で、AIは `%` `_` のエスケープが未定義であることに気づいた。**
> **だが `確認事項.md` に上げず、リスク節に「今回はエスケープなしで実装し、勝手に仕様を作らない」と書いて進んだ。**
>
> **エスケープしないことも仕様の決定**なのに、**「気づいたが進む」と書ける場所を用意したせいで、聞かずに済ませられた。**
>
> **リスク節に入ってよいのは2種類だけ** —— **内部構造の判断**（層の分け方・命名・ライブラリ選択）と、
> **やってみないと分からないこと**（この書き方でバリデーションが効くか）。
> **外から見える振る舞いが決まっていないものは、リスクではなく `確認事項.md` へ。**

> [!note]- Playbook の `plan.md` の実物
> ```
> ## Files that change
> portal/src/claims/StatusPanel.tsx (new), claims-api/routes/status.py, ...
> ## Order of work
> 1. Add the status endpoint behind existing auth. ...
> ## Risks
> The claims-core API rate-limits at 50 rps; the panel must cache.
> ## Proof
> test_status.py covers the four claim states; screenshot matches the approved mock.
> ```

**(2) 計画を詰問する**

**Playbook の "How to execute it" の2手目。3つ聞く。**

> **"Interrogate the plan by asking what could break, which step is riskiest, and what alternatives were rejected"**

| 聞くこと | なぜ効くか |
|---|---|
| **何が壊れうるか** | 計画が触っていない範囲への影響が出る |
| **どの手が一番危ないか** | 順序の妥当性が確かめられる |
| **★捨てた代替案は何か** | **AIが黙って決めたことを吐かせる**（`06`「聞かなければ言わない」） |

**★3つめが本命。** 「捨てた代替案」を聞かないと、**選ばれなかった方は永久に見えない。**

**(3) AIが自分で決めた箇所を、1つずつ処理する**

| 印 | 意味 | どうする |
|---|---|---|
| **聞いて設計書を直した** | 仕様管理者に聞いて答えが出た | **設計書に書き戻す** |
| **決まらないと知って進む** | 下の2つだけ | **`確認事項.md` に「未回答」で残す** |

**「決まらないと知って進む」に入ってよいのは2つだけ**

| | 例 |
|---|---|
| **聞いても決まらなかった** | Tag削除の順序が FK の不在に依存。**DB定義書は複合PK、DDLは制約なし。どちらが正本かは仕様管理者しか決められない** |
| **時間切れで聞けなかった** | 部分一致の `%` `_` の扱い |

**★「聞けば決まる」ものがここに並んでいたら、A が甘い。**
Todo名の物理名（DB定義書は `NAME`、DDL は `title`）は**聞けば答えが出る**ので、ここには入らない。**評価の議論はそこを突く。**

**⚠ 新しいセッションでやる。** 同じセッションだと、**会話の中で口頭で埋めた情報がAIに残っている**ので判定にならない。**渡したファイルだけで成立するかを見たい。**

> [!warning]- Playbook の `answered or carried forward` をそのまま持ち込まないこと（2026-09-01）
> Design のゲートに **"are the open questions from `intent.md` answered or carried forward?"** とあるが、
> **これは `intent.md` の open questions に掛かっている。発案者の疑問なので機能単位。**
> **実装者が聞きたい粒度とは違う**（「content は30文字か100文字か」を product owner が次に先送りすることはない）。
>
> **Build 段階の停止条件は別物で、こちら。**
> **"Iterate until an engineer who has never seen the conversation could implement the change from the plan alone."**
> **質問が片付いたか、ではなく、計画が実装できるか。**だから研修も「質問の仕分け」ではなく
> **「AIが自分で決めた箇所を把握しているか」**にした。

### 実装計画の雛形は配らない

**節の名前を `CLAUDE.md` に1行書くだけにする。**

> **実装計画は `../todo-app-api-docs/実装計画.md` に「変更するファイル／作業の順序／リスク」の3節で書いてください。**

**配布物を増やさない**（8/31 指摘1）。**AIに出させるものなので、雛形を配ると「埋める作業」に見えてしまう。**

### 用語 — 個有名以外は日本語にする

**研修生に渡す言葉は日本語。** Playbook の引用は出典なので原文のまま（この文書は講師用）。

| Playbook | 研修での名前 |
|---|---|
| `plan.md` | **`実装計画.md`** |
| Files that change / Order of work / Risks | **変更するファイル／作業の順序／リスク**（**`Proof` は落とした**） |
| `spec.md` | **配られた設計書** |
| Open questions | **`確認事項.md`** |
| **`CLAUDE.md` / `.claude/settings.json`** | **変えられない。**Claude Code が読むファイル名 |

**★2段に畳んだ（2026-09-04）。名前を混ぜない。**

| | 何を持つか | いつ | 誰が |
|---|---|---|---|
| **`テストケース一覧.md`** | **入力・事前状態・期待する結果・根拠** | **A（手5）** | AIが作り、研修生が抜け漏れを見る |
| **テストコード** | ケースをコードにしたもの | B | AI |

**以前は間に「受入条件」と「実装計画の証明節」があり、同じ情報が4段あった。**
**受入条件は実務で使わず、粒度も定まらないので落とした**（`15_通しの流れ.md` A章の折りたたみ）。

---

## この一例が「良い」と言える根拠

**A の良し悪しは C で数として出る**（`12` の評価節）。**この6手を打つと、こう減る。**

| C で出るもの | どの手が効くか |
|---|---|
| **`確認事項.md` の「いつ＝実装中」の行**（AIが勝手に決めた箇所） | **手3**（既定の洗い出し）が最も効く |
| **根拠の書けないケース／`確認事項.md` に戻せていないもの** | **手5** |
| **C-3 で「AIだけが見つけた」もの** | **手1**（アーキ図の注記）・**手4**（前提を書く） |
| **A で印をつけていなかった穴** | **手3・手6** |

**議論では、この4つの数を材料にする。**
「あなたのアプローチはどうだったか」だけだと感想戦になるが、
**「B で AI が勝手に決めた箇所が7件あった。どこを渡していれば減ったか」なら具体になる。**

---

## Playbook から、あえて外したもの

**水谷さんへの説明にも使える。**

| Playbook にあるもの（プレイ名） | 外した理由 |
|---|---|
| **Skills as institutional knowledge** | **配布物が増えると「シンプルにする」から遠ざかる。**`CLAUDE.md` 1枚で足りる（保留。長くなったら再考）。**Playbook 自身が "Keep it under a page" と言っているので、超えたら分ける、が判断基準になる** |
| **Claude Code on auto mode** | **`12` で「auto は禁止」にしている。**流れを見せるのが B の狙いなので**正面から逆** |
| **Hooks**（承認ゲート・保護パス） | **新規に教える技術になる**（設計原則5）。1週間に入らない |
| **worktree で並列セッション** | **「任せた仕事の流れを見せる」B の狙いと逆。**新人には早い |
| **Subagent（verifier）** | 同上。**C は自分の手で確かめさせたい** |
| **Evals**（`CLAUDE.md` 変更の回帰テスト） | 資産を育てる話。**到達ライン「1年」の内容**（`06`） |
| **MCP / CI-CD / デプロイ段階の権限分離** | **新人の裁量外**（`06`「軸にしないもの」） |
| `intent.md` / `REVIEW.md` | 課題として与える／2か月目のレビュー観点で代用 |

**Playbook は「組織が AI-native に移行する」ための全体像で、研修は「新人1人がチケット1本を回す」範囲。**
**入口（何を渡すか）と出口（何を確かめるか）だけを取り、組織側の仕掛けは全部落とした。**

---

---

# B 任せる

**別に書くことが無い。** `CLAUDE.md` の4手がそのまま模範で、**研修生が選ぶ余地はほぼ無い。**

**★示すとしたら、実測1つだけ。**

> **試走では9コミット・79テスト・1時間27分。**
> **「では実装に着手して」以降、人間の入力は承認5回だけだった。**

**B で人がやるのは、②の赤い出力を見ることだけ。**ここを飛ばされたら止める。

> [!note]- 偽の緑が1回出た（2026-09-08・試走）
> **Tag 1件取得の `notFound` が、ルーティングがまだ無いため偶然404で通った。**
> AI自身が「**ルーティング自体が無いため偶然404で通っている状態**」と言い、**テストを強めずに進んだ。**
>
> **「赤を見た」だけでは足りない。全部が赤かを見る必要がある。**
> **これが C-2「壊して確かめる」がなぜ要るかの実例になる。**

---

# C 確かめる

**2026-09-10〜11 に1周回した実測をもとに書いている。**

## ★ 全体を通して効いたのは、指示の1文だった

**同じ作業でも、投げ方で出力の質が変わる。**試走で効いたのは3つ。

| | 効いたこと |
|---|---|
| **「出力をそのまま見せてください」** | **生の `git diff --stat` 45行が逐語で出た。**要約も省略もなし。**入れないと、突き合わせたふりができる** |
| **「推測で埋めないこと。見つからないものは見つからないと書く」** | **メソッド名46個すべてが実ファイルに実在。捏造ゼロ。**曖昧なものは「兼用」と注記して押し込まなかった |
| **「別セッションで」** | 実装したセッションには**「自分が何を変えたか」が残っている**ので、**実物を見ずに記憶で答える** |

---

## C-1 機械チェックの妥当性を確かめる

### 手a ケース一覧とテストの対応表を作らせる

**AIにやらせてよい。**69件を人が目で追う価値は薄い。

> **`テストケース一覧.md` と `src/test/` を突き合わせて、ケース1件ごとに、対応するテストメソッドがあるかを表にしてください。**
> **テストメソッド名は実際のファイルから引用してください。推測で埋めないこと。**
> **対応が見つからないケースは、見つからないと書いてください。**

**実測（2026-09-10）:** **1分49秒。**テストクラス10本を**全文 Read**（grep で済ませていない）。
**69行ちょうどの対応表。**メソッド名46個すべて実在。**「テストが無い」1件を検出。**

**★検出できたのは、fixture まで開いたから。**「紐づきありのTodoを削除する」ケースに対し、
**テストが `todoId=1`（紐づきなし）を消していた**ことを、`setup.xml` と `afterDelete.xml` を読んで突き止めている。
**メソッド名の一覧だけを見た講師は、これを外した。**

### 手b 敵対的にテストを検査させる（ミューテーションテスト）

**実装を機械的に変異させて、テストが落ちるかを見る。**「テストのテスト」。

**Java の定番ツールは PIT（pitest）**だが、**`pom.xml` に依存を足す**ので課題の前提と衝突する。
**→ 別セッションのAIに探させる。**道具を増やさずに同じことができる。

> **`src/main/` の実装を1箇所だけ書き換えて、`mvn test` が緑のまま通る変更を探してください。**
> **実際に書き換えて `mvn test` を実行し、緑のままだったものだけ報告してください。**
> **1件ごとに、どのファイルの何行目をどう変えたかを示すこと。**
> **終わったら `git checkout .` で戻し、`git status` がクリーンなことを確認してください。**

**⚠ 最後の1文が要る。**壊したまま放置されると次に進めない。

**★講師は1回実演する。**試走では `TodoService.delete` から `todoMapper.deleteTodoTagsByTodoId(todoId);` を
**1行消して、79件すべて緑のまま**だった。**A-2 で一番手間をかけた裁定（FK・削除順序）の成功パスが、無傷で通る。**

> **「79件全部緑なのに壊れている」を全員が同時に見る**ほうが、各自でやるより効く。

**⚠ 壊す行は「テストが無いと分かっている機能」から選ぶ。**
`limit` で試そうとしたが、**JOIN を足すと先に別のテストが重複で落ちて**、示したいことが示せなかった。
**→ 手a の結果が、そのまま手b の指定に使える。順番に意味がある。**

---

## C-2 コードレビューをする

**★ここだけはAIを使わない。**模範アプローチも無い。

**C-2 と C-3 は同じ作業を別の主体がやるもので、差そのものが材料**になる。
**C-2 で AI を使うと C-3 が空になる。**そして**研修全体で、自分の目でコードを読む工程はここだけ。**

**★示せるのは、読む単位だけ。**

> **PR は1本のまま、コミット単位で読む**（`git show <commit>`）。

**差分が大きいほど見落とす。**実測で、**AIレビューの F1 は差分サイズとともに 0.657 → 0.043 まで崩れる**
（arXiv:2606.15689）。GitHub 公式も「**大規模・複雑な変更ほど見落とす**」と明記している。**人も同じ。**

**★B でコミットをエンドポイント単位に刻んだ理由が、ここで回収される。**刻んだ単位が、そのままレビューの単位になる。

---

## C-3 AIにもレビューさせ、仕分ける

### 投げ方

> **`gh pr diff <番号>` の出力を読んで、コードレビューをしてください。**
> **設計書は渡しません。**差分だけを見て、バグ・設計上の問題・テストの弱さを指摘してください。
> **指摘ごとに、根拠となる行を `ファイル名:行番号` で示してください。**推測で書かないこと。

**「設計書は渡さない」は実務条件に合わせたもの。**PR上で動くAIレビューは、別リポの設計書を見られない。

**実測（2026-09-11）:** **4分21秒。**差分3352行を7回に分けて**最後まで読了**。
**設計書は開いていない**が、**本体ソースは8回読んだ**（Service・Controller・Entity・例外クラス・テスト）。
**指摘4件。重大度ラベルは付けなかった**（指示していないため）。

> [!warning]- ⚠ 「問題が無いことの説明は書かなくて結構です」は逆効果だった
> **ノイズを減らすつもりで入れたが、AIは締めくくりにこう書いた。**
>
> > **問題なしと判断した箇所（…ロールバック検証テスト、境界値テストの網羅性など）は上記の通り特に指摘なし。**
>
> **「境界値テストの網羅性」を明示的に「問題なし」側に分類した。**——**穴がそこにあるのに。**
> **「触れていない」が「問題ない」に化けた。**
>
> **★逆側の失敗も実在する。** クラスメソッドの事例では、**AIレビュー69件中52%が「重大な指摘なし」で、
> その「無いことの説明」だけで約10万字**になり、トリアージ型に作り替えている。
>
> **→ 折衷案（未検証）:**
> > **見た範囲と、見ていない範囲を分けて書いてください。「問題なし」と「見ていない」を区別すること。**

### 仕分けと処置

| 判定 | 処置 |
|---|---|
| **正しい** | **直す → コミット → push → 再レビュー。**直すときも **B の4手** |
| **的外れ** | 理由を書いて閉じる |
| **判断できない** | **★消さない。**仕様なら `確認事項.md`（確認時）へ、技術なら**そのままPRに書いてレビュアーに上げる** |

**★再レビューは1周で切る。**2周目の新しい指摘は仕分けるだけ。
**Claude Code の Code Review が「nit が無限に積み上がる／再レビューのたびに新しい nit が増える」を
既知のノイズ源として公式に挙げている。****収束しない。**

**⚠ 仕分けはAIに書かせない。**本人の判断そのもの。
**試走では、仕分けをAIに投稿させようとして止まった**——`振り返り.md` と同じく、**記録はワークスペースの外にあるので読めない。**

### ★ 実測: 人とAIで、見つけたものの重なりがゼロだった

| | 件数 |
|---|---|
| 自分だけが見つけた | **3**（`%` `_` のエスケープ／`limit` のテストの弱さ／紐づきありTodoの削除テストが無い） |
| AIだけが見つけた | **4**（TOCTOU／404と409の優先順位／DELETE の `@RequestBody`／`limit=abc` の型不正） |
| **両方が見つけた** | **★0** |
| 的外れ | **0** |

| | 何を見ていたか |
|---|---|
| **人** | **仕様との対応**（裁定した内容・ケース一覧・fixture の中身） |
| **AI** | **コードの構造**（並行性・HTTPの作法・入力の型） |

> ### 「AIレビューは検証手段ではないが、拾い漏れの受け皿にはなる」
> **受け皿になった4件は、全部こちらが見ていない領域だった。**

### ★★ 出なかったものが、一番の情報だった

**A-2 で「エスケープする」と裁定したのに設計書へ書き戻さなかった1件は、全工程を素通りした。**

| 工程 | 拾ったか |
|---|---|
| A-3 別AIレビュー | ✗ 設計書に痕跡が無いので原理的に出ない |
| B-0 実装計画 | **リスク欄に書いて、自分で「なしで実装する」と決めた** |
| C-1 突き合わせ | ✗ ケース一覧に無い |
| **C-3 コードレビュー** | **✗ 差分に4箇所あり、3352行を最後まで読んで、一言も触れていない** |

> ### 「設計書に書き戻す」がなぜ必須かの実証。
> **書き戻さなければ、どこにも引っかからない。**

**★「的外れの数＝A で渡し漏れた情報の量」は、指標として機能しなかった。**
試走では**的外れ0件**。設計書を渡していないのに、**AIが仕様に踏み込む指摘をしなかった**ため。
**代わりに効いたのは「AIが触れなかったもの」。**

---

## 未決

| | |
|---|---|
| **示し方** | **議論の場で口頭**か、**この文書を配る**か。配ると期をまたいで流出する |
| **手3 の表現** | 「フレームワークの既定」は新人に通じるか。言い換えが要るかもしれない |
| **手数** | 6手は多いか。**研修生に配る手順ではない**ので厚くてよい、と判断している |

---

## 付録 — 「実装までに達成しておくこと」を素で洗い直した（2026-09-01）

**Playbook を読み込む過程で、道具の話が先に立ち、目的が見えにくくなった。**
**そこで、この検討の文脈を一切渡さない状態で3つの視点から洗い直した。**

| 視点 | 問い |
|---|---|
| **一般論** | 他人が書いた設計書一式を渡された新人が、着手前に達成しておくべきことは（ソフトウェア工学の一般論として） |
| **AI特有** | AIに実装をまるごと任せる場合、人が書く場合と比べて追加で要るもの／不要になるもの |
| **品質逆算** | 実装後に「担保できた範囲／できない範囲」を線引きするには、実装前に何が決まっている必要があるか |

### 3つとも同じ理由に行き着いた

> **事後に作った基準は実装に合わせて曲がる。**（一般論）
> **出てきてから決めると、出てきたコードの構造に引きずられて観点自体が汚染される。**（AI特有）
> **参照点を実装から作れば差分は定義上ゼロになる。**（品質逆算）

**実装前の作業は「早くやるため」ではなく、「汚染される前に固定するため」。**

### 結論 — 2種類・5つ

| | 何が起きるか | どれ |
|---|---|---|
| **決まらないと書けないもの** | **止まる。**あるいは勝手に決まる | **3・5** |
| **あとから作ると汚染されるもの** | **書けるが、根拠として使えなくなる** | **1・2・4** |

**5つの本体は `12_研修の流れ_説明用.md` の A 章冒頭。**

### 洗い直しで見つかった抜け

| | どう埋めたか |
|---|---|
| **範囲（やらないこと）が無かった** | **`受入条件.md` に「対象外」節を足した。**C-1 の「頼んでいないファイルが変わっていないか」は、**頼んだ範囲を書いていないと言えない** |
| **矛盾の扱いが曖昧だった** | **矛盾は聞いて設計書を直して消す。**「決まらないと知って進む」に入れない |

> [!note]- 「正本の優先順位を先に合意する」案は採らなかった
> 一般論の視点からは、**「型は DB定義書が正、項目名は API仕様が正」といった優先順位を先に合意しておけば、**
> **軽微な矛盾は自力で裁定でき都度止まらずに済む**、という案が出た。大規模なら有効。
> **採らない理由:** 資料6つ・矛盾数件で**直せる規模**。そして優先順位ルールは**矛盾を残す前提**なので、
> **「実装と設計をずらさない」（8/31 水谷さん）と逆を向く。**仕組みが2つになるのも避けたい。

### 手として取り入れたもの

| 取り入れたもの | どこへ |
|---|---|
| **「実装するとして、あなたが独断で決めることになる事項を列挙して。実装はしないで」** | **手6 の1手目。**AIは生成が得意で自分から止まるのが苦手。その性質を逆に使う。**⚠ 列挙ゼロは準備完了の証拠にならない** |
| **ゲートを「準備の完全性」から「間違いが安く分かるか」へ** | **B を7コミットに刻む理由づけ。**準備の不足は**周回数で買う** |
| **「一般解から外れるところだけ書く」** | **手4。**AIは書いていない部分をコーパスの平均で埋めるので、**平均どおりの部分は書かなくてよい** |
| **「整って見える誤り」** | **C の「テストを壊して確かめる」の理由づけ。**人の手抜きは荒れて見えるが、**AIの取り違えは自然で一貫して見える。読んで違和感がないことは正しさの証拠にならない** |
