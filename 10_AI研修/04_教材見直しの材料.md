# 2か月目の教材見直しの材料

> 更新: 2026-08-06 / 由来: AI研修の実験（`02_実験_プロンプト差の検証.md` P1）の副産物
> きっかけ: 講師の所感「AIの出力は**研修で扱うコードよりモダンで読みやすい**」。これが本当か調べた結果。

---

## 1. 結論: 「古い」のは一部だけで、多くは教育的単純化。むしろ研修コードの方が設計は厚い

### バージョンは古くない

| | 研修デモ | AI生成 |
|---|---|---|
| Spring Boot | **3.4.1** | 4.0.7 |
| Java | 21 | 21 |
| MyBatis Starter | 3.0.4 | 4.0.1 |
| その他 | Flyway 9.21.2 / MapStruct 1.5.5 / DBUnit / Mockito / Security / Actuator | docker-compose 同梱 |

3.4.1 は**教材を作った時点の最新**であり、通常の講義ラグ。「古い」とは言えない。AIが 4.0.7 を使ったのは「AIは常に最新で書く」という性質の表れで、**研修環境に持ち込むと壊れる方が問題**（`02_実験` §4.1 の欠陥①）。

### 「古い」と言えるのはこの2点だけ

| 箇所 | 現状 | 今なら |
|---|---|---|
| DTO / Form / Entity | class ＋ 手書き getter/setter（`TagPostFrom` / `TagEntity`） | `record`（AIは全DTOをrecordで書いた） |
| DI | 明示的コンストラクタ ＋ 「コンストラクタインジェクション」のコメント | `@RequiredArgsConstructor`（pom に Lombok はあるのに未使用） |

**ただしどちらも教育的単純化の可能性が濃厚。** 手書き getter/setter は「フィールドとアクセサの対応」を体で覚えさせる意図、明示コンストラクタは「コンストラクタインジェクションとは何か」を見せる意図だろう（実際コメントがそう示唆している）。**古さと教育的配慮を混同しないこと。**

---

## 2. 逆に、研修コードの方が優れている点（こちらが重要）

| 観点 | 研修デモ | AI生成 |
|---|---|---|
| **層の分離** | `controller / logic.service / mapper / entity / form / query / queryCondition` ＋ `util`。**検索条件を独立層に切っている** | controller で `@RequestParam` を素朴に受けるだけ |
| **エラー設計** | `ErrorCodes` enum ＋ `TodoRuntimeException` 基底クラスでエラーコード体系を管理 | `@ExceptionHandler` を並べるだけ。しかも `ex.getMessage()` を外に漏らす |
| **Javadoc** | 全メソッドに `@param` / `@return` 付き。初学者が「何を書くべきか」を型で学べる | ほぼ無し（AIは自明なコードにコメントを書かない傾向） |
| **規約の強制** | `todo_checks.xml`（CheckStyle）同梱 | lint / format 設定なし |

### 「AIのコードが読みやすい」のは、設計が浅いからでもある

層が少なく抽象が少なければ読みやすい。**読みやすさと実務的な正しさは別**。研修コードが `query`/`queryCondition` を独立層に切り、エラーコードを enum で体系化しているのは、大規模APIに近づけるための意図的な複雑さ。

→ **この非対称性はそのまま教材になる**（→ §4）。

---

## 2.1 ★実物の不備が8件見つかった（AIが指摘・2026-08-06）

P2b の実験で、AIが**設計書を読んで矛盾と穴を指摘してきた**（`02_実験` §4.2）。**教材の実際の不具合**なので、AI研修とは切り離して対応が要る。

### ① `content` の最大文字数が文書間で食い違っている ★要修正

| 文書 | 記載 |
|---|---|
| **要件定義書**（Todo課題1／課題2） | **30文字以内** |
| DB定義書 | `content varchar(100)` |
| OpenAPI（`todo.yaml`） | `maxLength: 100` |
| プログラム設計書（`Todo機能.md`） | 「100文字以内」 |

**3対1で100が正**と思われるが、**要件定義書だけが30**。実DBも `varchar(100)` で確認済み。

**なぜ今まで表面化しなかったか（推定）**: 研修生はDB定義とプログラム設計に従って100で実装すれば動くので、**矛盾に気づかないまま通過している**可能性が高い。逆に要件定義書を素直に読んだ研修生は30で実装し、レビューで指摘される — という理不尽が起きうる。

→ **要件定義書の記載を100に直すか、他を30に直すか。どちらが正か確認して統一する。**

### ② Tag削除時の `todo_tag` クリーンアップが未定義

Todo削除時は `todo_tag` の削除が設計書に明記されているが、**Tag削除時の記載がない**。そのままだと**孤児レコードが残る**。

→ 設計の穴。仕様として追記するか、「あえて書いていない（研修生に考えさせる）」のかを決める。

### ③ ★プログラム設計書が N+1 を指示している（最優先）

`03.API設計/プログラム設計/Todo課題2/Todo機能.md`「Todo の一覧取得」:

```
- 取得した結果が 1 件以上の場合
  - 取得した Todo 情報リスト(要素(todo))分、ループ
    - todo に紐づく Tag の情報を取得
```

登録・更新でも「`tags` の各要素についてループし、Todo と Tag の紐づきを登録」＝**ループ内 INSERT**。

**レビュー観点の筆頭項目「for 文の中で SQL を呼び出していないか」と正面から矛盾している。**

研修生に起きていること（推定）:
1. プログラム設計書に従ってループで実装する
2. レビューで N+1 を指摘される
3. **「設計書通りに書いたのに」**

→ **設計書を直すか、「設計書はナイーブな手順書であり、実装時は N+1 を避ける」と明記するか。** 後者なら「設計書の指示を鵜呑みにしない」という教育的意図として成立するが、**どこにもそう書かれていない**ので現状は単なる矛盾。

### ④ `pom.xml` に `maven-checkstyle-plugin` が無い

コーディングガイドは「レビューを投げる前に必ず Checkstyle によるプロジェクトへの解析を行うこと」と要求しているが、**`pom.xml` にプラグインが無く、CLI からは実行できない**。IDE プラグイン経由でしか動かせない状態。
→ プラグインを追加するか、ガイドに「IDEから実行する」と明記する。

### ⑤ `@MapperScan` / `@Mapper` がどこにも無い

`develop` の scaffold には `@MapperScan` も無く、Mapper インタフェースも空フォルダなので、**研修生が `@Mapper` を付けないと起動しない**。AIは起動失敗して自力で気づいた。
→ 意図的に気づかせる設計なら問題ないが、**どこにも案内がない**。`TodoApplication` に `@MapperScan` を置くか、講義で触れるかを決める。

### ⑥ `system_conf.properties` がどこからも import されていない

`todo.linkedTagLimit=5` が定義されているのに `spring.config.import` が無く、**`LimitContext` の値が 0 に解決される**。結果、タグ紐づけが常に 400 になる。**P2b・P3 の両方でAIが同じ問題を見つけて修正した**（＝確実に踏む地雷）。
→ ⑤と同じ判断。意図的なら案内を、そうでなければ scaffold を直す。

### ⑦ Tag紐づけ上限の source of truth が二重

`todo.yaml`（課題2）が `maxItems: 5` をハードコードしている一方、scaffold は `LimitContext`（`todo.linkedTagLimit`）を持っている。**どちらを正とするか設計書に書かれていない。**

### ⑧ OpenAPI の path param が `string`、DB は `Integer`

`todoId` / `tagId` が OpenAPI では `string` 型。非数値IDを渡したときの挙動が設計書に無く、**汎用の 500 ハンドラに落ちる**（400/404 にならない）。

---

## 2.2 追加で7件（実測・2026-08-21）

**段①（把握）の実測**で、`Todo課題2` に対して同じ問いを投げた結果（`10_シミュレーション/01_把握_出てくるもの.md`）。**§2.1 に無かったものだけ**を挙げる。

| | 不備 | 種類 |
|---|---|---|
| ⑨ | **`todo` の Todo名の物理カラム名が食い違う。** DB定義書 `todo.md` は `NAME`、DDL は `title`、OpenAPI も `title` | 矛盾 |
| ⑩ | **`todo_tag` の制約が食い違う。** DB定義書は両カラムに PK◯ NN◯、**DDL は `todo_id integer, tag_id integer` のみ**（PK・NN・FK なし）。同じ紐づけの二重登録を誰も止めない | 矛盾 |
| ⑪ | **201 のレスポンスが食い違う。** プログラム設計は「TodoId と 201 を返却」、**OpenAPI の 201 は `headers.Location` のみで `content` が無い** | 矛盾 |
| ⑫ | **`tags` に同じ `tagId` が重複したときの扱いが無い。** `uniqueItems` の指定も無く、件数一致による存在チェックだと重複指定は必ず 404 になる | 空白 |
| ⑬ | **`todoResponse.tags` の並び順が未定義。** 順序が明記されているのは Todo一覧（todoId昇順）と Tag一覧（tagId昇順）だけ | 空白 |
| ⑭ | **要件定義に「進捗状況」が残っている。** 課題2の Todo更新に「Todo名と、進捗状況、紐づけるタグを変更できる」。DB設計・API設計・プログラム設計に対応物が一切無い | 残骸 |
| ⑮ | **テスト作成手順書と雛形が別の層を指している。** 手順書（`04.開発資料/追加課題/`）は `logic/service/...`、雛形の空フォルダは `app/controller/{tag,todo}`。`logic/service` のテストフォルダは存在しない | スコープ違い |

## 2.3 さらに6件（実測2回目・2026-08-21・教材修正後）

**§2.2 の後に不備を直し、同じ問いを再実行した結果。§2.1・§2.2 に無かったものだけ。**

| | 不備 | 種類 |
|---|---|---|
| ⑯ | **登録時の `version` の初期値が決まらない。** DB定義書の VERSION は「初期値 0」だが、**DDL は `version integer not null` で DEFAULT が無く**、プログラム設計も version に一切言及していない | 空白 |
| ⑰ | **同名の Tag を重複登録できるか不明。** 要件定義・プログラム設計に記述なし、**`CREATE TABLE tag` に UNIQUE 制約なし**、重複に対応するエラーコードも無い | 空白 |
| ⑱ | **`tag.yaml` の `name` に `minLength`/`maxLength` が無い。** 同ファイルの 400 の説明は「1文字以上30文字以内」で、`todo.yaml` の `title` には制約が入っている | 矛盾 |
| ⑲ | **トランザクション境界が定義されていない。** プログラム設計は「Todo更新→楽観ロック→Todo_Tag を削除して再登録」と複数SQLを並べるが、**docs 全体に「トランザクション」の記述が無く、`@Transactional` の実例も既存コードに1つも無い** | 空白 |
| ⑳ | **`queryCondition` パッケージの役割が資料に無い。** 雛形に `queryCondition/{todo,tag}/` があるのに、要件定義のアーキテクチャ図は `Form`/`Query`/`Entity` の3種にしか言及せず、**docs 全体に `queryCondition` の記述が無い** | 空白 |
| ㉑ | **部分一致検索での `%` `_` のエスケープが未定義。** 要件定義・OpenAPI・プログラム設計のいずれにも特殊文字の扱いの記述が無い | 空白 |

**⑯〜㉑ の6件はすべて講師が実機で確認済み。** 実測2回とも**AIの誤検出は0件**だった。

## 2.4 さらに5件（段②の実測・2026-08-21）

**AIに「実装の計画を出させた」ときに出たもの**（`10_シミュレーション/02_決定_出てくるもの.md`）。**計画は実装に近いので、段①では出ない質問が出る。**

| | 不備 | 種類 |
|---|---|---|
| ㉒ | **★テスト用DBが開発用と同じ。** `src/main` と `src/test` の `application.properties` がどちらも `jdbc:postgresql://localhost:5432/todo` を指している。**テストを実行すると動作確認用のデータが壊れる** | **環境の不備（要対応）** |
| ㉓ | **空白のみ（`" "`）を未入力とみなすかの規定が無い。** `@NotBlank` と `@NotNull`+`@Size` で挙動が変わる。**`BoundaryValue.ONE_SPACE_CHARACTER` がテスト側に用意されているのに、判定の規定が無い** | 空白 |
| ㉔ | **`limit` の上限値が無い。** 下限1のみ規定 | 空白 |
| ㉕ | **定数クラスの置き場と命名が無い。** コーディングガイドは「対応の定数クラスから呼び出す」と要求するが、**Todo/Tag 用の定数クラスが存在しない**（`util/statics` と `core/exception/statics` は別用途） | 空白 |
| ㉖ | **`todo_tag` の SQL / Mapper の置き場が無い。** 「紐づけは Todo 機能側で行う」とあるだけで、`TodoMapper` に相乗りか `TodoTagMapper` 新設かは書かれていない | 空白 |

**㉒ は講師が事前に直す。** 段③で全員が踏む（`07_実施計画.md` §5 の準備物）。**㉓〜㉖ は段②の題材として残す。**

### ★AIの出力は毎回違う（研修設計に効く）

**3回とも同じ教材に同じ問いを投げているのに、全部に出たのは「Tag削除時の `todo_tag`」1件だけ。**

| | 08-06（P2b・8件） | 08-21 1回目（10件） | 08-21 2回目（10件・修正後） |
|---|---|---|---|
| Tag削除時の `todo_tag` | ○ | ○ | **○** |
| `content` の文字数 ／ `system_conf` の配線 ／ 上限の二重管理 | ○ | ○ | **修正済みで消滅** |
| N+1 を指示する設計書 ／ `@MapperScan` ／ `pom` に CheckStyle ／ path param が `string` | ○ | — | — |
| カラム名 ／ `todo_tag` の制約 ／ `tags` の重複 | — | ○ | **○** |
| 201 の返却 ／ 進捗状況 ／ `tags` の並び順 | — | ○ | **—** |
| `version` の初期値 ／ 同名Tag ／ `tag.yaml` の長さ制約 ／ トランザクション境界 ／ `queryCondition` ／ LIKE エスケープ | — | — | **○** |
| テストの層 | — | ○（矛盾） | **○（空白に変化）** |

**含意が4つある。**

1. **模範解答（答えの一覧）は作れない。** 研修生ごとに違うものが出るので、**講師は「全量」を手元に持ち、出てきたものを判定する**形になる。→ **§2.1＋§2.2＋§2.3 が講師の手元の全量**
2. **「AIに読ませれば漏れなく出る」は誤り。** 1回では出ない。**複数回・複数の切り口で回してようやく揃う**（`08_ギャップ` の多角走査と同じ話）
3. **★教材を直しても題材は減らない。** 3件を潰したのに**件数は10件のまま**で、新規6件が出た。「確認しないと決められないこと」は仕様の深さに対して常に残る
4. **★矛盾を直すと空白が前面に出る。** 1回目は矛盾6／空白3、2回目は**矛盾3／空白7**。しかも2回目の空白は**トランザクション境界・エスケープ・テスト範囲**という、より実務的で判断が難しいものに寄った。**教材を整えるほど演習の質が上がる**

### 直したのに消えなかった1件（修正の効果の評価）

**⑮（テストの層）は手順書を削除しても消えず、種類が変わっただけだった。**

| | 1回目 | 2回目 |
|---|---|---|
| 種類 | **矛盾**（手順書は Service 層、雛形は Controller 層） | **空白**（テスト方針の資料が無い） |
| AIの言い方 | 「どちらに合わせるのが成果物か判断できない」 | 「雛形は Controller だけなのに**モック用の依存（`mockito-inline`）も入っている**。必要なテスト範囲を示す資料がない」 |

**AIは pom の依存からも意図を読もうとする。** 削除だけでは足りず、**参照実装1本＋DoD が必須**であることの裏付けになった（`07_実施計画.md` §5 の準備物）。

### AI研修用にローカルで直したもの（2026-08-21）

**この材料は2か月目リポジトリへは反映しない。** AI研修用の教材として独立させる方針に決まったため（`09_配布物/README.md`）。
**この節は「2か月目チームへの申し送り」として使う。** 直した内容がそのまま改修の候補一覧になる。
作業場は `Desktop/ai-sim` の使い捨てクローン、ブランチ `ai-training-fixes`。

| 対象 | 直したこと | 元の番号 |
|---|---|---|
| `todo-app-api` | `LimitContext.java` ／ `main`・`test` の `system_conf.properties` ／ `test` の `todo.tagLimit=5` を**削除**（どこからも参照されていない死んだ配線） | ⑥⑦ |
| `todo-app-api-docs` | 要件定義（課題2）の `content` を **30 → 100**（他3点が100） | ① |
| `todo-app-api-docs` | 壊れていた目次を実際の見出しに合わせ、重複見出しを区別 | — |
| `todo-app-api-docs` | `tag.yaml` の `info.title: addtional-tag` → `tag`、summary の追加課題への言及を削除 | — |
| `todo-app-api-docs` | **`プログラム設計/Todo課題2/Tag機能.md` を新規追加**（既存 `Todo機能.md` の書式・詳細度に揃える） | — |

**残したもの**: ②⑧⑨⑩⑪⑫⑬⑭。**実装判断に効く不備は段①の題材として価値がある**ので消さない（`07_実施計画.md` §3）。**消したのは「作業を止める不備」だけ。**

**⑮（テスト手順書の層）は直していない。** 手順書自体は「追加課題実施時に利用」と明記されており**それ自身は誤りではない**ので、**AI研修側で層を固定する**（参照テスト1本を Controller 層で配り、指示ファイルに1行書く）方が筋。

### 2か月目への改修案（AI研修とは別作業）

| | 内容 |
|---|---|
| 1 | **固定値を設定ファイルへ分離する仕組みを、動く形で入れ直す。** 今回は死んだ配線を削除しただけなので、**設定外部化そのものは改修案として残る** |
| 2 | 上記の「ローカルで直したもの」を本体に取り込むか判断する |
| 3 | 残した矛盾（⑨⑩⑪⑭）を教材として直すか、AI研修の題材として残すかを決める |

---

### この発見のしかた自体が使える

**AIに設計書一式を読ませて「矛盾や抜けを挙げさせる」だけ**で出てきた。人間が何年も気づかなかったものが1回で出る。
→ **他ユニットの設計書（yafh-app-api など）にも同じことをやる価値がある。**
→ **研修の演習としても組める**（`01_方向性の候補.md` 案A）。研修生にもできる作業で、しかも「AIは仕様の矛盾を見つけられる」という有用性の実感になる。

---

## 3. 見直し候補（優先度つき）

| | 内容 | 判断 |
|---|---|---|
| **高** | DTO / Form を `record` に切り替える | Java 21 確定の教材で class＋手書きアクセサを続ける理由は薄い。ただし「record の前にまず class で構造を理解させる」回を残すなら温存も可 |
| **中** | Mapper の戻り値に `Optional` を導入（`findById` 系 ＋ `orElseThrow`） | null 安全の講義に直結する。模範解答を書くときに教材化する価値あり |
| **中** | 例外処理を二層で教える | 独自 `TodoRuntimeException` 階層は本格的だが複雑。`@RestControllerAdvice` ＋ `@ExceptionHandler` という**業界標準パターンも並行して教える**と、配属後に読むコードが分かりやすくなる |
| **低** | Lombok の使用方針を明記 | pom に依存はあるのに Tag 機能では未使用。「いつ Lombok を使ってよいか」を教材に書く |
| **低** | Spring Boot のバージョン追随ルール | 教育効果は薄いが、EOL・脆弱性管理として年1回の更新ルールを検討 |

---

## 4. AI研修への含意（`01_方向性の候補.md` 案Aを強める）

研修生はおそらく「**AIのコードの方が読みやすい**」と感じる。それは正しい感想だが、**読みやすさは設計の浅さでもある**。

そこで案Aの演習にこう組める。

> 自分たちが `query` / `queryCondition` を独立層に切ったのはなぜか。
> エラーコードを enum で体系化したのはなぜか。
> AIの出力にはそれが無い。**その差を説明できるか。**

**設計を理解しているかのテストになる。** しかも「AIは使えない」でも「AIの方が優秀」でもない、第三の着地になる — **AIは読みやすく浅いものを速く作る。深くするのは人間の仕事**。

---

## 5. この比較の限界

現在チェックアウト中のブランチは **Tag 機能のみ実装済みで、Todo 機能は `.gitkeep` のみ**（演習課題として空）。そのため日付型・コレクション操作・複雑な WHERE 句の MyBatis 記法は研修側に現物がなく、直接比較ができていない。

→ **正確な比較には `lecture_*` の段階別デモブランチか模範解答を見る必要がある。** §1〜2 の判断は Tag 機能からの類推を含む。
