# 実験: プロンプト品質による差の検証（案Aの前提確認）

> 更新: 2026-08-06 / ステータス: **Stage 1 実施前**
> 目的が2つある。①社長の要望「投げるプロンプトによって出る差を経験させる」が教材として成立するか ②案A（検証・レビュー型）の前提「AIはもっともらしく間違える」が本当か。
> **段階的に進め、各段階の終わりで「ここまでやる」を判断する。** 全部やる前提では組まない。

---

## 0. 実験の骨格

**同じ課題を、プロンプト品質を変えて3回解かせ、出力の差を測る。**

> **2026-08-06 重要な訂正**: 研修生は**ゼロから作らない**。`todo-app-api` の **develop ブランチを clone** し、**研修側が用意した枠**（パッケージ構成・使うクラスの指定・エラーハンドリング機構）に実装を埋めていく。
> P1 でゼロから作らせたのは**研修の条件とは違う**。P2 以降は develop の枠を起点にする。

| 条件 | 与えるもの |
|---|---|
| **P1 雑**（実施済） | 一行の指示だけ。設計書も枠も渡さない。**研修の条件とは違う**ので、位置づけは「導入の驚き」用（§4.1） |
| **P2 研修生と同じ** | **develop ブランチの枠** ＋ 要件定義書 ＋ DB定義 ＋ DDL ＋ OpenAPI ＋ エラー設計 |
| **P3 コンテキスト厚め** | P2 ＋ **コーディングガイド** ＋ **AI向けプロジェクト指示ファイル（`todo-app-api-develop/CLAUDE.md`）**。**プロンプトは P2 と完全に同一**（2026-08-06 再改訂・§2参照） |

> **本命は P1 vs P2 ではなく P2 vs P3。** P1（雑）の出力は品質が低くならず「きれいな間違い」が出たため、品質での差が付かなかった（§4.1）。差が出るのは「**研修生が読めるコードかどうか**」で、それは規約とコンテキストを渡すかどうかで決まる。

対象は **課題1（Todo単体CRUD＋楽観ロック）→ 課題2（Tag追加・多対多）までフル**。

### 統制（守らないと実験が無意味になる）

**渡してよいもの / 絶対に渡してはいけないもの**（2026-08-06 訂正）

| | 対象 | 理由 |
|---|---|---|
| **渡してよい** | `todo-app-api` の **develop ブランチ**（＝研修生が clone する枠） | 研修生に渡っているものなので、渡さないと**研修の条件を再現できない** |
| **渡してはいけない** | **`answer-sample*` ブランチ**（`review\todo-app-api` に存在）、研修生の提出ブランチ `task/NNNNNN-氏名` | **模範解答＝配布厳禁の秘匿物**。読ませたら実験が無意味になるだけでなく、扱いとして不適切 |
| **渡してはいけない** | `MyWork\code\02-api\00.todoApiDemo\todo-app-api` の完成実装と `lecture_*` の段階別デモブランチ | 講義デモ＝実質の答え |

> `review\todo-app-api` に `lecture_*` は存在しない（デモ側のリポジトリにある）。review 側の答えは **`answer-sample*`**。ブランチ名で判断すること。

### ⚠ AIは渡していない情報源を勝手に探しに行く（2026-08-06 実測）

P2 の実行中、AIが **`C:\aevic_dev\review\docs\todo-app-api-docs\04.開発資料 / 05.講義資料 / 06.参考資料` を自分から探して読みに行った**。P2 には渡していない資料（コーディングガイド＝P3の材料、講義資料）である。

**原因**: `~/.claude/settings.json` の `permissions.additionalDirectories` に `C:\aevic_dev\review` が入っている（`bootstrap.ps1` が設定）。**このマシンのどのセッションからも review が読める。**

**より深刻な経路**: `review\todo-app-api` の `.git` には **`answer-sample*`（模範解答）** がある。`git show answer-sample:<path>` で**答えに到達できる**。

**対策（3層。上ほど確実）**

1. **`~/.claude/settings.json` の `additionalDirectories` を実験中だけ外す**（キー名を `_additionalDirectories` などに変えて無効化 → 実験後に戻す）。**これが唯一確実な手段**
2. **実験ディレクトリに `.claude/settings.json` を置き `permissions.deny` で塞ぐ**（`Read(//c/aevic_dev/**)` ／ `Documents/aevic` ／ `Documents/MyWork` ／ `Bash(git:*)`）。deny は allow に優先するはずだが、**Bash 経由の読み取りまで完全には塞げない**
3. **実験ディレクトリに `CLAUDE.md` を置いて範囲を明示**（「ここに置かれているものが渡された情報のすべて。足りなければ探さず報告せよ」）。協力的なエージェントには効くが強制力はない

**実行後は必ずログを検査**し、範囲外の読み取りがあればその試行は汚染として扱う（記録シートに明記）。

> **P1 は汚染されていない**と判断できる。AIが作ったテーブルは `todos`（`title` 200文字 / `description` 1000文字）で、実際の設計書の `todo`（`title` 30文字 / `content` 100文字）と**フィールド名も桁数も違う**。設計書を読んでいれば一致していたはず。

### この事象は研修設計上のリスクでもある（重要）

**研修生がAIを使うと、模範解答への経路が構造的に開く。**

研修生は `todo-app-api` を clone しており、その `origin` には **`answer-sample*` がある**。`git fetch origin answer-sample` → `git show` で、AIは答えを取得できる。**研修生が意図しなくても、AIが「参考になりそうな情報」として自分から取りに行きうる**（今回まさにそれが起きた）。

→ 対策は `03_研修環境とガードレール.md` §8 へ。

- 出力先はスクラッチパッドなど作業用の場所。**会社リポジトリには書き込まない。**
- develop を起点にする場合も、**別の場所にコピーして作業させる**（元の clone を汚さない）。
- やり取りの回数と、詰まった箇所をその都度メモする。**後から思い出せない。**

---

## 1. 与える材料の在り処

すべて `C:\aevic_dev\review\docs\todo-app-api-docs\` 配下。

| 何 | パス |
|---|---|
| 課題1 要件定義書 | `01.要件定義\Todo課題1\Todoアプリ要件定義書.md` |
| 課題2 課題定義書 | `01.要件定義\Todo課題2\Todoアプリ課題定義書.md` |
| DB定義 | `02.DB設計\DB定義書\todo.md` / `tag.md` / `todo_tag.md` |
| DDL | `02.DB設計\ddl\01_create_database.sql` 〜 `03_create_role.sql` |
| API設計（課題1） | `03.API設計\openapi\01.Todo課題1\todo.yaml`（8KB）／`tag.yaml`（6KB） |
| API設計（課題2） | `03.API設計\openapi\02.Todo課題2\todo_tag.yaml`（10KB） |
| エラー設計 | `03.API設計\エラー設計書\APIエラーコード.md` |
| プログラム設計 | `03.API設計\プログラム設計\Todo課題1\Todo機能.md`／`Todo課題2\Todo機能.md` |
| コーディングガイド | `04.開発資料\コーディングガイド.md` |

CheckStyle設定とレビュー観点は docs 外にある。

| 何 | パス | 注意 |
|---|---|---|
| CheckStyle設定 | `C:\aevic_dev\review\todo-app-api\todo_checks.xml` | **このxmlファイルだけ**取り出す。同じリポジトリの `src` 配下は実装＝答えなので見ない |
| レビュー観点 | `lecturer-vault\02_API研修_2か月目\04_補足\レビュー観点\API研修_レビュー観点の指針.md`／`API研修_レビュー観点引継ぎ用.md` | Stage 2 の評価軸。**P3で渡すのはこちら** |

Stage 3 で使う材料（先に在り処だけ把握しておく）:

| 何 | パス |
|---|---|
| 動作確認シナリオ | `lecturer-vault\02_API研修_2か月目\postman動作確認用テストシナリオ\動作確認ver2\テスト用セットアップsql.md` |
| Maven実行時のエラー集 | `lecturer-vault\02_API研修_2か月目\04_補足\過去にあった不具合\maven実行時のエラー.md` |
| トラブルシューティング | 同 `API研修のトラブルシューティング＆ナレッジ.md` |

---

## 2. プロンプト文面（そのまま使う）

### P1（雑）

```
Spring Boot と MyBatis で Todo 管理の REST API を作ってください。
PostgreSQL を使います。
```

以上。追加の質問には答えず、AIが勝手に決めたことをそのまま観察する。

### P2（研修生と同じ）＝ **develop の枠を起点にする**

**先に `develop` を別の場所にコピーしてから、そこで作業させる**（元の clone を汚さない）。

```
このリポジトリは新人研修の課題用テンプレートです（todo-app-api の develop ブランチ）。
空のフォルダに実装を埋めて、Todo 管理の REST API を完成させてください。
添付の設計書に従ってください。
- 要件定義書
- DB定義書
- DDL
- OpenAPI（API設計書）
- エラー設計書（APIエラーコード）

既存のコードとフォルダ構成を確認し、それに合わせて実装してください。
設計書に書かれていないことは、書かれていないと明示してください（勝手に決めないでください）。
```

2つの仕掛けがある。

- **「設計書に書かれていないことは明示してください」** — 勝手に決めた箇所を自己申告させると後の評価が楽になる。そして**申告漏れがあること自体が観察対象**（P1 では桁数もエンドポイントも黙って決めた）
- **「既存のコードとフォルダ構成を確認し、それに合わせて」** — これだけで規約を察するかを見る。**P3 で明示するまでもなく守れるなら、それが最大の発見**

> **P2 で最も注意して見るところ**（実験の分岐点）
> ① **`core/exception` の既存機構を使うか、`@RestControllerAdvice` で自作するか。** P1 では自作した。枠を見せれば使うか
> ② **`record` / Lombok / Stream を使ってしまうか。** pom に Lombok 依存があるので使う余地がある
> ③ 7層（controller/entity/form/logic.service/mapper/query/queryCondition）に素直に従うか、勝手に簡略化するか
> ④ Flyway のマイグレーションSQL が無いことに気づいて指摘するか、勝手に作るか、Docker で立てようとするか
> ⑤ Javadoc を書くか（既存の完成品クラスには付いている）

### P3（コンテキスト厚め）＝ **実務にある形のコンテキストを足す**

> **2026-08-06 再改訂の理由**: 当初は「講師用のレビュー観点指針」を渡す設計にしていたが、**あれは人間を教育するための資料で、実務には存在しない形式**。実験としても、後半に N+1 の解決パターン（IN句・JOIN のコード例）が載っているため「レシピを渡したから直った」になり何も測れない。
> **実務でAIに渡すのは次のもの。**

| | 現物 | P3 での扱い |
|---|---|---|
| コーディング規約 | `04.開発資料/コーディングガイド.md` | **追加した** |
| **AI向けプロジェクト指示ファイル** | `CLAUDE.md` / `.cursorrules` / `.github/copilot-instructions.md` | **`todo-app-api-develop/CLAUDE.md` として追加した** ← 本命 |
| 静的解析の設定 | `todo_checks.xml`（CheckStyle） | develop に同梱済み（P2b にもあった） |
| PRテンプレート | `.github/PULL_REQUEST_TEMPLATE.md` | develop に同梱済み（P2b にもあった） |
| 既存コードそのもの | develop の枠 | P2b で効果を実証済み |
| 講師用レビュー観点 | — | **渡さない**（実務に存在しない形式） |

**プロンプトは P2 と完全に同一にする。** 差はファイルだけ。これで「差の正体はプロンプトではなくコンテキストである」ことが実験デザインとして示せる。
コーディングガイドへの導線は、プロジェクトの `CLAUDE.md` 冒頭に「`04.開発資料/コーディングガイド.md` と併せて読むこと」と書いてある（実際のプロジェクトと同じ形）。

### 置いた `todo-app-api-develop/CLAUDE.md` の内容（要点）

「このプロジェクトのリーダーが書いたもの」として、実務の体裁で書いた。

- **技術スタック** — Boot 3.4.1 固定、`pom.xml` は変更しない
- **パッケージ構成と責務** — 7層それぞれの役割を表で。「この構成から外れるパッケージを新設しない」
- **使わないもの（チームの習熟度に合わせた制約）** — `record` / Lombok / Stream API / PATCH。**理由を「チームに Java の学習途上のメンバーが含まれ、全員が読めることを最優先とする」と書いた**。実務でも「チームの習熟度に合わせる」は実在する制約なので、研修事情を実務の言葉に翻訳した形
  - 「既存の完成済みクラスの一部で Lombok を使っているが、新規実装では使わない。既存分は移行対象として残しているだけ」と補足 → **明示的な指示と既存コードの実例が矛盾する状況を意図的に作った**
- **SQL / MyBatis** — **「for 文の中で SQL を発行しない（N+1）」の一行だけ**。解法（IN句・JOIN）は書かない
- **エラーハンドリング** — `core/exception` の既存機構を使う。新しい例外ハンドラを作らない
- **コメント** — 全 public メソッドに Javadoc
- **設定ファイル** — 変更したら理由を報告する
- **分からないとき** — 推測で埋めず、何が書かれていないかを報告する

### P3 の測定点（優先順）

1. **N+1 が消えるか。** 一行の規約で直るか。**直れば「コンテキスト整備が効く」（主軸①）、残れば「規約を渡しても直らない＝人間のレビューが要る」（主軸②）。どちらでも研修の根拠になる**
2. **明示的な指示 vs 既存コードの実例、どちらが勝つか。** Lombok は指示で禁止、既存コードでは使用中。**P2b では既存コードに従った**。指示で覆るか
3. `deleteTodo` の削除順序は直るか（`CLAUDE.md` に書いていないので、たぶん残る）
4. `validateTagsExist` の重複 tagId 誤爆は直るか（同上）
5. **設計書の矛盾（`content` 30 vs 100）を再度指摘するか** ← **部分的な再現性テストになる**
6. 設定ファイル（`spring.config.import`）の追加を再度やり、報告するか

> **P3 で確かめたいこと**: P2（読めないコードが出る）と P3（読めるコードが出る）の差が、**「研修生が読めるか読めないか」という形で劇的に出るか**。出れば、これが社長の「プロンプト差」要望に対する最良の答えになる（P1 vs P2 の差は薄かった）。

---

## 3. 段階と、各段階での判断

### Stage 1 — 生成させる

3条件 × 課題1 → 課題2 でコードを書かせる。

**記録するもの**: やり取りの回数 / 体感の所要時間 / 生成されたファイル数と層構造 / AIが質問してきたか、黙って決めたか / 詰まった箇所

**この段階で分かること**: AIが研修生の課題をどこまで書けるか。**研修生の1か月が何分で溶けるか。**

**判断すること**: プロンプト品質で差が出たか。差が出なければ、社長の「プロンプトによる差」案は**教材として薄い**（＝濃くしなくてよい根拠になる）。差が出れば教材になる。

---

### Stage 2 — 読んで評価する ★案Aの核心

生成物を、研修生と同じ道具で評価する。

| やること | 使うもの |
|---|---|
| 要件充足の確認 | 要件定義書の項目を1つずつ○× |
| レビュー観点を当てる | `02_API研修_2か月目\04_補足\` の既存観点 |
| 静的解析 | CheckStyle（`todo_checks.xml`）、SonarLint |
| 破綻箇所の特定 | 特に **MyBatis の XML（`resultMap`/`<collection>`）／楽観ロック（versionカラム）／バリデーション／エラーコードの対応** |

**この段階で分かること**: **AIがどう間違えるか。** 案Aの演習（欠陥探し）が成立するかは、ここで判明する。

**判断すること**: 見つかった欠陥が、研修生が自分のレビュー観点で見つけられる種類か。見つけられないほど巧妙／逆に自明すぎるなら、案Aの組み方を変える必要がある。

**成果物**: 「AI出力の欠陥カタログ」初版。これが案Aの教材の芯になる。

> **予想（外れたら記録する）**: 課題1は素直な題材なのでAIはかなり書ける。差が出るとしたら**課題2の MyBatis XML マッピング**。`<collection>` を使った1対多の取得は学習データが薄いはずで、動くが N+1 になる、あるいは重複行を潰せていない、といった形で崩れるのではないか。

---

### Stage 3 — 動かす（やるか未定）

Maven build → PostgreSQL 準備 → 起動 → Postman/ThunderClient で叩く。

**この段階で分かること**: **「動くけど間違っている」を実証できるか。** 案Aの一番強い教材はこれ。静的に読んで分かる欠陥より、動いてしまう欠陥の方が怖いと伝わる。

**判断すること**: Stage 2 の結果を見てから決める。Stage 2 で十分に欠陥が出ていれば、動作確認は研修の中でやらせればよく、講師の事前検証としては不要かもしれない。

---

### Stage 4 — テストを書かせる（やるか未定）

AIに DBUnit + JUnit のテストを書かせ、**そのテストが何を保証しているかを評価する**。

**この段階で分かること**: AIが書いたテストは通るが、**カバーしているのが自明なケースだけ**ではないか。3か月目の必須課題（テスト仕様書でのケース洗い出し）と直結する論点。

**判断すること**: 研修に入れるか。ここは「AI研修」ではなく「3か月目のテスト課題の改訂」の話になる可能性もある。

---

## 4. 記録シート（Stage 1・2 用）

|                  | P1 課題1                                                             | P2 課題1 | P3 課題1 | P1 課題2 | P2 課題2 | P3 課題2 |
| ---------------- | ------------------------------------------------------------------ | ------ | ------ | ------ | ------ | ------ |
| やり取り回数           | autoでやったので、１回                                                      |        |        |        |        |        |
| 体感所要時間           | メモリの貧弱なPCなので、30分くらい                                                |        |        |        |        |        |
| 生成ファイル数          | 10くらい                                                              |        |        |        |        |        |
| 層構造は正しいか         | 一般的なCURDで３層アーキテクチャくさい、プロジェクト構成も分かりやすい                              |        |        |        |        |        |
| 要件充足（○/項目数）      | 何も指定してないので、tag機能の実装は当然ナシ                                           |        |        |        |        |        |
| 楽観ロック実装          | なし                                                                 |        |        |        |        |        |
| バリデーション          | 良しなに                                                               |        |        |        |        |        |
| エラーコード対応         | RestControllerAdviceで                                              |        |        |        |        |        |
| MyBatis XML の正しさ | tag機能の実装まで指示しないと間違えるようなとこまでこないですね。                                 |        |        |        |        |        |
| CheckStyle 違反数   | 試してない                                                              |        |        |        |        |        |
| レビュー観点での指摘数      |                                                                    |        |        |        |        |        |
| 勝手に決めた箇所         | 仕様                                                                 |        |        |        |        |        |
| その他              | autoでsonnet使用<br>勝手にdocker desktopでpostgresql環境作成<br>テストも勝手に回してくれた |        |        |        |        |        |

---

## 4.1 P1 の結果と考察（2026-08-06）

**実施条件**: Sonnet / auto モード / やり取り1回 / 約30分 / 出力 `C:\Users\kanamaru\Desktop\実験\p1\todo-api`

### 講師の所感

> 予想通り。指示していない内容は実装してこない。ただ**破綻がなく、研修で扱うコードよりむしろモダンで読みやすい**。プロジェクト構成もシンプルでよい。
> **ただし「要件を入れないとこうなりますよ」という体感が、意味のあるものかどうか怪しい。**

### 良かった点（指示していないのに満たしていた）

3層分離／**DTO と Entity の分離**（DTOは record）／**トランザクション境界が Service の public メソッド**（クラスに `@Transactional(readOnly=true)`、更新系に `@Transactional`）／**`SELECT *` を使わずカラム列挙**／`resultMap` を明示／ポートを 5433 にして既存の 5432 との衝突を回避。
**既存レビュー観点の主要項目を、指示なしで半分程度満たしている。**

### 致命的な欠陥3つ（研修生が自力で気づけない類）

| # | 欠陥 | なぜ効くか |
|---|---|---|
| 1 | **pom が Spring Boot 4.0.7**（研修は 3.4.1）。依存も `spring-boot-starter-webmvc` など Boot 4 系の名前 | 「**AIは最新版で書く**」の実証。研修環境に持ち込むと壊れ、原因が自力で分からない |
| 2 | **テストが `contextLoads()` 1本だけで実質空**。動作確認は curl で軽く叩いただけ（下記ログ参照）。それでもAIは「テストを作成し実行しました」と報告する | 「テストが通った」を信じてよいかという論点。**Stage 4 の主題が P1 で既に観測できた** |
| 3 | `@ExceptionHandler(Exception.class)` が **`ex.getMessage()` をそのままレスポンスに返す** | 情報漏洩の典型。DB由来のメッセージやテーブル名が外に出る |
| **4** | **POST のレスポンスと、直後に GET したレスポンスの `createdAt` が一致しない**（下記） | **「動くけど間違っている」の実物。** 200が返るので動作確認では気づかない |

### 欠陥④の実物（curl ログ・2026-08-06）

```
POST /api/todos → {"id":2, ... "createdAt":"2026-08-06T16:37:39.9289138", ...}
GET  /api/todos → [{"id":2, ... "createdAt":"2026-08-06T16:37:39.928914", ...}]
                                                              ↑ 7桁 → 6桁に丸められている
```

**原因**: `create()` が `TodoResponse.from(todo)` で**メモリ上のオブジェクトをそのまま返している**（DBを読み直していない）。Java の `LocalDateTime` はナノ秒（9桁）精度だが、PostgreSQL の `timestamp` はマイクロ秒（6桁）精度なので、**保存時に丸められる**。

**なぜ教材として優れているか**:
- **動作確認では絶対に気づかない**。200 が返り、値も一見同じ
- クライアントが POST のレスポンスを信じて等価判定・キャッシュすると壊れる
- **楽観ロック（version）と組み合わさるともっと厄介**になる ＝ 2か月目の講義内容に直結
- 難易度が絶妙。並べて「どこが違う？」と聞けば研修生でも気づけるが、言われなければ気づかない

→ **この欠陥は Stage 3（動かす）でしか見つからなかった。** 静的に読むだけでは出てこない。**Stage 3 の価値が実証された**ので、講師の事前検証としても研修の演習としても入れる価値がある。

### AIが自分でトラブルシュートした痕跡

curl ログを見ると、AIは日本語が化ける問題に対し「shell のエンコーディングかアプリのエンコーディングかを切り分ける」ためにJSONを**UTF-8ファイルに書き出して `--data-binary` で投げ直している**。

→ **研修生が詰まるはずの箇所を、AIが自力で回避してしまう**という観察。文字化けは1か月目からの定番の詰まりどころだが、それを学ぶ機会が消える（`01_方向性の候補.md` §0「ジュニア育成モデルの崩壊」の具体例）。

その他: `updateCompleted` が SELECT を2回している（`getOrThrow` と `findById`）／Flyway ではなく `schema.sql` + `spring.sql.init.mode: always`／楽観ロックなし／`title VARCHAR(200)` 等を勝手に決定／エンドポイントが `/api/todos`。

### 予想の答え合わせ

- **的中**: 課題1は素直なのでAIはかなり書ける。MyBatis XML の破綻は課題2まで行かないと出ない（講師の観察も同じ）
- **外れ**: レビュー観点の充足度は想定より**高かった**。目立つ欠陥は少なく、**バージョン・空テスト・情報漏洩という「見えにくいところ」に集まっている**

### 最重要の発見: 「雑だと汚いものが出る」ではない

P1 で出たのは**質の低いコードではなく、質の高い「違うもの」**だった。要件を渡していないので tag も楽観ロックも作らないが、コード自体は破綻がなく読みやすい。

→ **「プロンプトが雑だと品質が落ちる」という筋書きは成立しない。** 成立するのは「**雑だと、きれいな間違いが出る**」という筋書き。そして**質が低ければ気づけるが、質が高くて要件と違うものは気づけない**ので、教訓としてはこちらの方が怖く、価値も高い。ただし伝え方が難しい。

→ **差を測る軸を「コード品質」から「要件充足」に変える必要がある。**要件定義書の項目を1つずつ○×で数えれば、P1 は半分も満たさないことが定量的に見える。品質で比べると差が出ない。

→ **P1 の位置づけを変える**: 「雑なプロンプトの悪い例」ではなく**「導入の驚き（30分でこれが出る）」用**に使い、プロンプト差の本命は **P2 vs P3**（要件は同じで、規約とコンテキストを渡すかどうか）に置く。

### 副産物: 2か月目の教材見直しの材料

「研修で扱うコードよりモダンで読みやすい」という所感は、AI研修とは別に**2か月目の教材そのものの見直し材料**になる（→ `04_教材見直しの材料.md`）。

---

## 4.2 P2b の結果と考察（2026-08-06）

**実施条件**: `C:\Users\kanamaru\Desktop\実験\p2b` / develop の枠（`git archive`・`.git` なし）＋ 設計書3種 / ガード3層あり（`CLAUDE.md` 込み）/ 起動確認まで実施。
**汚染なし**: `additionalDirectories` 無効化済み。AIの自己申告でも範囲外の参照なし（外部とのやり取りは Maven の依存取得と、8080 ポートのプロセス停止のみ）。

### 結論: **「既存コードを見せれば規約は伝わる」が実証された**

P1（ゼロから）と正反対の結果になった。

| 分岐点 | P1（ゼロから） | **P2b（develop の枠）** |
|---|---|---|
| エラー機構 | `@RestControllerAdvice` を**自作** | **既存の `core/exception` を完璧に使用**。`NotFoundException(ErrorCodes.NOT_FOUND_RESOURCE)` 等。既存ファイルは1バイトも改変なし |
| 層構成 | controller/service/mapper ＋ dto の実質3層 | **7層すべて使い分け**。`Query`（受け口）と `QueryCondition`（Mapperへ）まで分離 |
| record | DTO を全部 record | **使わず** class ＋ Lombok |
| Stream | `stream().map().toList()` | **使わず** for 文 |
| **Lombok** | `@Data @Builder @RequiredArgsConstructor` | **使った**（`@Getter @Setter @AllArgsConstructor`） |
| Javadoc | ほぼ無し | **全メソッドに `@param`/`@return`/`@throws`**。日本語コメントの体裁も既存に合わせた |
| Spring Boot | 勝手に 4.0.7 | **pom 無改変**（3.4.1 のまま） |
| 楽観ロック | 無し | **実装**（`version = version + 1 WHERE ... AND version = #{version}` → 更新0件で例外） |
| DB | Docker で 5433 に新規 | **既存の設定をそのまま使用** |
| テスト | 空の `contextLoads()` を作成 | **作らなかった**（指示していないため） |

### 唯一の例外が Lombok。しかもAIは正しい

既存の `ErrorCodes` と `LimitContext` が **`@Getter @AllArgsConstructor` を使っている**。AIは既存コードを正しく読んで、正しく真似た。
**「研修生に教えていない」という事実は、コードのどこにも書かれていない。**

→ **これが「見せれば伝わる」と「言わないと伝わらない」の境界の実例。**
→ 一般化すると: **既存コードに「有る」ものは伝わる。「無い」ことからは何も伝わらない。**（`record` を使わなかったのは既存に無いから、ではなく偶然かもしれない。P3 で切り分ける）

### 残った欠陥 — レビュー観点でしか捕まらないものばかり

| # | 欠陥 | 質 |
|---|---|---|
| **1** | **N+1問題**。`getTodoList` が Todo 一覧を1回取得したあと、**Todo 1件ごとに `selectTagsByTodoId` を発行**。100件なら101クエリ | **既存レビュー観点の筆頭項目**がそのまま出た。動くので動作確認では気づけない |
| 2 | **`deleteTodo` の削除順序**。先に `todo` を DELETE してから `todo_tag` を DELETE。**外部キー制約があれば失敗する順序**（研修では意図的にFKを外しているので通る） | 「制約のある環境なら壊れる」。実務移行時の地雷 |
| 3 | **`validateTagsExist` が重複 tagId で誤爆**。`[1,1,2]` を渡すと `selectTagsByIds` は2件返し、`found.size() != tags.size()` で **存在するタグなのに404** | エッジケース。テストを書かないと見つからない |
| 4 | **設定ファイルを勝手に変更**。`spring.config.import=classpath:system_conf.properties` を main/test 両方に追加 | **これは必要な修正**（無いと `LimitContext.linkedTagLimit` が解決できず起動失敗）。**develop の穴をAIが自分で塞いだ** |

> **予想の答え合わせ**: 「課題2の MyBatis `<collection>` マッピングで崩れる」は**外れた。崩れたのではなく、`<collection>` を使わずループで回避した。** 結果は動くが N+1。**「AIは難しい機能を避けて、動くが非効率な道を選ぶ」**という、より一般的で教えやすい失敗パターンだった。

### AIの最終報告（原文の要点・2026-08-06）

**auto モードで、一度も確認を挟まずに完走した。** 最後の報告に次が書かれていた。

**① 実地検証まで自分でやった**
> ローカルの実DB(Postgres)に対して起動確認し、一覧/1件取得・登録・更新・削除・**楽観ロック・バリデーションエラー**・タグ紐づけの全パスを curl で実地検証済み（**テストデータは後片付け済み**）

**② 既存ファイルの修正を自己申告した**（プロンプトの「書かれていないことは明示せよ」が効いた）
> `application.properties`(main/test) に `spring.config.import=classpath:system_conf.properties` を追加。`LimitContext` が読む `todo.linkedTagLimit` は `system_conf.properties` にしか定義されておらず、**かつどこからも import されていなかったため、このままではアプリ起動時にプロパティ解決エラーになる状態でした**

**③ 設計書どうしの矛盾を発見した** ★これが最大の収穫
> `content` の最大文字数について、**要件定義書（課題1・課題2）は「30文字以内」**、**DB定義書（`varchar(100)`）・OpenAPI（`maxLength: 100`）・プログラム設計書（「100文字以内」）は揃って100**。3つが一致する100を採用したが、**要件定義書側の記載が古い／誤りの可能性**があるため確認をお願いします

**④ 設計書に無いので実装しなかった点を明示した**
> - **Tag削除時に、そのTagを参照している `todo_tag` 行のクリーンアップ**（Todo削除時のみ設計書に明記されており、**Tag削除時の記載がない**）
> - テストコード（依頼されていないため対象外）

### ③④の意味 — AIが教材の品質検査をしてしまった

- **③は研修教材の実際の不備。** 要件定義書と他3文書が食い違っている。研修生はDB定義に従って100で実装すれば通るので、**矛盾に気づかないまま通過している可能性が高い**。→ `04_教材見直しの材料.md` へ
- **④の Tag削除時の `todo_tag` クリーンアップ未定義は、設計の穴**。孤児レコードが残る。これも実物の不備
- → **「AIに設計書を読ませると、人間が見落とした矛盾を見つける」**という、案Aに無かった価値が出た。しかもこれは**研修生にもできる**（AIに読ませるだけ）。演習として組める

### auto モードについて（設計上の論点）

**一度も確認を挟まずに完走した。** 実装・起動・curl での全パス検証・後片付けまで自律的に回した。

- 研修で auto を許すと、**研修生は「何が起きたか」を見ないまま成果物を得る**
- 一方、今回の③④のような**質の高い報告は、最後まで走らせないと出てこない**
- → **「auto で走らせてよいが、報告とログを必ず読ませる」**が落としどころか。`03_研修環境とガードレール.md` §7（「何を渡したか記録する」の実体は未決）と接続する

### 案Aへの影響 — 演習の焦点を移す必要がある

**P2 の時点で規約適合がほぼ完璧**なので、「規約違反を探す」演習は**成立しにくい**。

→ **演習の焦点を「規約適合チェック」から「レビュー観点による設計欠陥の発見」へ移す。**
→ これは**難易度が上がって良い方向**。既存のレビュー観点資料（N+1が筆頭）がそのまま評価軸になる。研修生が2か月目に教わったことが、そのまま武器になる
→ `01_方向性の候補.md` 案A Day 3 の記述を「規約チェックリスト」から「レビュー観点による欠陥発見」に寄せる

### P3 の狙いも変える

当初の P3 は「規約を守らせる」だったが、**P2b の時点で規約はほぼ守れている**。P3 で見るべきは:

1. **レビュー観点（N+1を避ける等）を渡したら、N+1 を自分で回避するか。** `<collection>` を使うか、`selectTagsByTodoIds` で一括取得するか
2. **「Lombok を使うな」と明示したら守れるか**（＝「無い」ことを言葉で補えるか）
3. コーディングガイドと CheckStyle を渡したときの差

### 社長の「プロンプト差」要望への答え（ほぼ出た）

**差は出る。ただし軸は「コード品質」ではなく「既存コードベースへの適合」。**

- **P1（雑）→ P2（枠＋設計書）: 劇的な差。** 別物と言ってよい
- P2 → P3: レビュー観点で N+1 が消えるかどうか

**教材としてこの上ない題材**。「プロンプトを工夫すると良いコードが出る」ではなく、**「既存の文脈を渡さないと、正しくても使えないものが出る」**。SES現場に直結する。

---

## 4.3 P3 の結果と考察（2026-08-06）

**実施条件**: `Desktop\実験\p3` / Sonnet 5・effort high・auto / **プロンプトは P2b と完全に同一** / 条件差は `04.開発資料/コーディングガイド.md` と `todo-app-api-develop/CLAUDE.md` の2ファイルのみ / 起動して実HTTPリクエストで検証済み。

### 結論: **コンテキストは効く。ただし「書いたことだけ」が直る**

| 測定点 | P2b | **P3** | 判定 |
|---|---|---|---|
| **N+1** | ループ内で `selectTagsByTodoId` を発行 | **`IN (...)` で一括取得 → Java 側で `Map` にグルーピング**。書き込み側も `<foreach>` でバルクINSERT | **直った** |
| **Lombok** | 全クラスで使用 | **新規19ファイルすべて未使用**。明示コンストラクタ＋手書き getter/setter | **直った。明示的な指示が既存コードの実例に勝った** |
| マジックナンバー | `setVersion(0)` をハードコード | **`INITIAL_VERSION = 0` を定数化** | 直った（コーディングガイドが効いた） |
| `pom.xml` | 無改変 | 無改変 | 維持 |
| Javadoc | 全メソッド | 全メソッド | 維持 |
| **`deleteTodo` の削除順序** | `todo` → `todo_tag` の順 | **同じ** | **直らない**（指示に書いていない） |
| **重複 tagId で 404** | 同じ挙動・**無申告** | 同じ挙動・**自己申告した** | 挙動は同じだが**報告が出た** |

→ **「見せれば伝わる」の範囲がさらに狭まった。正確には「書けば直る／書かなければ直らない」。**
→ 一行の規約（「for 文の中で SQL を発行しない」）で N+1 が直った。**解法を書かなくても直る**。コンテキスト整備の費用対効果は高い。

### ★重大な発見: プログラム設計書が N+1 を指示していた（P2b の解釈を訂正）

`03.API設計/プログラム設計/Todo課題2/Todo機能.md` の「Todo の一覧取得」に、こう書かれている。

```
- 取得した結果が 1 件以上の場合
  - 取得した Todo 情報リスト(要素(todo))分、ループ
    - todo に紐づく Tag の情報を取得
    - 取得した Tag 情報を todo に付与
```

登録・更新でも「`tags` の各要素についてループし、Todo と Tag の紐づきを登録」＝**ループ内 INSERT** を指示している。

**つまり P2b の N+1 は「AIの欠陥」ではなく「設計書に忠実に従った結果」だった。**
§4.2 で「AIは難しい機能を避けて動くが非効率な道を選ぶ」と書いたが、**これは誤り。AIは設計書通りに実装しただけ**であり、P2b の出力は**正しい**。悪いのは設計書。

**P3 の AI はこの衝突を検知して報告した**:
> 課題2のプログラム設計は Todo ごとにループしてタグを取得すると書かれており、N+1 禁止のルールと衝突する。**1回の `IN (...)` で全件取得してから Java 側でグルーピングすることで両方を満たした**

→ **AIが仕様の矛盾を検出し、両方を満たす解を選んだ。** これは実務で最も価値の高い判断のひとつ。

### 研修生に何が起きているか（要確認）

1. 研修生はプログラム設計書に従ってループで実装する
2. レビューで N+1 を指摘される
3. **「設計書通りに書いたのに」** となる

**教材が自己矛盾している。** レビュー観点（N+1禁止）とプログラム設計書（ループを指示）が食い違っている。心当たりがあるか確認が必要。

### AIが自分で踏んで自分で直したバグ

> 最初に書いた `<association>` ベースの resultMap に適切な `<id>` が無く、**同じ Todo に対する複数の tag 行が1つのオブジェクトに潰れた**（後のタグが前のタグを黙って上書きした）。`TodoTagEntity` をフラットな列に直し、Java 側で `TagEntity` を組み立てる方式に変更して修正

**§4.2 で私が予想した「`<collection>` で重複行を潰す」バグを、AI が実際に踏んだ。** そして自力で検知・修正した。研修生が同じところで詰まる可能性が高い箇所。

### 再現性の揺れ（重要）

| | P2b | P3 |
|---|---|---|
| `spring.config.import` の追加 | main **と** test の両方 | **main のみ**（test 側は未修正） |
| 診断の説明 | 「起動時にプロパティ解決エラーになる」 | 「limit が 0 に解決され、タグ紐づけが 400 で失敗する」 |
| `@Mapper` の付与 | （P2b でも必要だったはず。要確認） | **「scaffold に `@MapperScan` も `@Mapper` も無く起動失敗したので追加」と明示** |

**同じ問題を見つけても、直し方と説明が揺れる。** test 側の import が漏れているのは P3 の方が不完全。
→ **教材にするなら、講師が事前に生成した固定の出力を配るのが安全。**研修生各自に生成させると、手元の成果物が揃わない。

### 案Aへの影響

- **主軸①（コンテキストを整える）は強く実証された。** 一行の規約で N+1 が直り、明示的な禁止で Lombok が消えた
- **主軸②（検証）の題材は、当初想定と別のところにある。** 「AIの欠陥」ではなく「**教材／設計書の欠陥**」が本命。`deleteTodo` の削除順序と重複 tagId 誤爆は残ったが、これらも設計書が触れていない箇所
- **主軸③（AIに検査させる）が最も収穫が大きかった。** 設計書の矛盾・穴が計7件（下記）出た

---

## 5. この実験自体が教材になる

講師が初めてAIを触る過程そのものが一次資料になる（`00_前提と目的.md` §2-D）。
**うまくいかなかったこと・想定と違ったことを必ず残す。** 研修生も同じところで詰まる。
